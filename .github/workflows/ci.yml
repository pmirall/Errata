name: ci

on:
  push:
  pull_request:

jobs:
  host-tests:
    name: host tests (g++)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run host test suite
        run: make -C tests check
