# tools/sprites/atlas.txt - THE ATLAS ORDER.
#
# One set name per line, in the order they are emitted into
# Pebblebol/src/data/sprites_pebbles.h. Blank lines and `#` comments ignored.
# `<name>` here means the file `tools/sprites/<name lowercased>.txt`, and that
# file must declare `name: <NAME>` - the two are one identity, checked both ways.
#
# THE ORDER IS A CONTRACT, not a convenience, which is why it is a committed
# file and not os.listdir(). Two things depend on it:
#
#   1. FIXED SLOTS COME FIRST. EGG_IDLE must be id 0: sprite_set() falls back to
#      set 0 for any out-of-range id, so slot 0 is the one that may never move.
#
#   2. THE SPECIES BLOCK IS THE TAIL, in roster order, with no gaps. The pack's
#      invariant is `sprite_id == id - 1` (tools/content/species.json,
#      data/species_table.h), so a body's slot is DETERMINED by its species id:
#      PB_SPRITE_BODY_FIRST + (id - 1). gen_sprites.py refuses a species block
#      that is out of order, has a hole, or names a species the pack does not
#      have - because a body in the wrong slot draws the wrong creature on HOME
#      and every other check in the tree stays green.
#
# P9-C3 appends 60 lines below the eggs, one per species, in id order.

EGG_IDLE
EGG_CRACK

# The two POSE sets.  They carry no `species:` key because they are not a
# creature: sprite_set_id() returns one of these for POSE_SLEEP / POSE_SICK at
# EVERY stage and for EVERY species, exactly as the legacy atlas did with its
# seven pose sets, and P9-C3 records why in data/sprites.h's LOOKUP banner.
SLEEP
SICK

# --- species bodies, one per roster id, PB_SPRITE_BODY_FIRST + (id - 1) ------
# P9-C3 registered all 60. The order below IS the roster order and is checked
# both ways by check_species_binding(): entry k of this block must declare
# `species: k+1`, and its name must be the pack's name for that id folded by
# slug(). Do not sort this list, do not group it by family, do not append to
# it - a new species goes at its id's position or the generator refuses.

# family 1 - PACKET
PAKETO
FRAGMAR
RAFAGON

# family 2 - RADIO
BIPPO
ESTATIC
JAMRON

# family 3 - LATENCY
LAGUI
JITERA
TIMAUT

# family 4 - PIXEL
PIXIO
ARTEFAX
BURNIX

# family 5 - SPAM
SPAMITO
KADENAX
BLAKLIX

# family 6 - BUG
BUGGO
EXPLOID
ROOTKAR

# family 7 - WORM
WORMI
PARASIX
PLAGON

# family 8 - PHISH
KARNADA
KLONIX
ESTAFEX

# family 9 - NULL
NULIX
VOIDINA
SEGFALT

# family 10 - BITROT
BITTO
FLIPIX
PODRIX

# family 11 - DAEMON
DAEMI
SERVIK
KERNON

# family 12 - FIREWALL
PROXI
GATEON
MURAX

# family 13 - MEMORY
KACHI
MEMORO
LEKRON

# family 14 - FILE
FILITO
ARKIVO
ZIPBOM

# family 15 - PING
PINGO
FLOODRA
DENYRA

# family 16 - GLITCH
GLITCHI
ERROX
PANIKA

# family 17 - PORT
PORTU
SKANOR
BAKDORA

# family 18 - CRYPTO
KLAVIK
CIFRAX
RANSORA

# family 19 - PROBE
PROBIX
BEAKON
TWINIX

# family 20 - COOKIE
COOKIT
TRAKKAR
PANOPTIX
