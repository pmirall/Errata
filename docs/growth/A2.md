# RECON A2

## Lo que ya existe
THE EFFECT ENUM IS COMPLETE AND FULLY IMPLEMENTED. `AttackEffect` has 16 slots (`Errata/src/data/attacks_table.h:39-57`), generated from `balance.json`'s `EFFECT_ENUM` by `tools/gen_content.py:1024`. `battle.cpp::apply_effect()` has a live arm for every one of the 16 (`Errata/src/game/battle.cpp:644-702`), guarded by `static_assert((int)ATK_EFF_COUNT == 16, ...)` at `Errata/src/game/battle.cpp:705-707` so a 17th slot fails the build rather than resolving to "nothing happened". There is NO implemented-but-dead effect in the engine.

THE CONTENT IS ALSO FULLY REACHABLE - the audit trail in the tree that says otherwise is stale. Measured over `tools/content/{attacks,species}.json`: all 34 attack ids appear on at least one of the 60 shipped learnsets; 0 orphans. `Errata/src/data/species_table.h` emits all 60 rows (`ROSTER_FAMILIES = 20`, `tools/gen_content.py:73`), and `tests/test_content.cpp:533` already pins `unreachable == 0` with `CHECK(used[13]); CHECK(used[22]);`. TWO COMMENTS STILL DESCRIBE THE OLD 36-species prefix and are now false: `tests/test_battle_golden.cpp:33-38` ("It CANNOT cover ATK_EFF_DOT... species 46, outside the shipped 36-species prefix") and `tests/test_battle_golden.cpp:235-237`. Species 46 Glitchi knows attack 13 (DOT); species 60 Panoptix knows 22 (Firewall).

EFFECT USAGE, COUNTED (34 rows): NONE 12; BUFF_ATK 3; BUFF_DEF 3; PROTECT_HALF 3; BUFF_SPD 2; and NINE effects used EXACTLY ONCE - DEBUFF_ATK (21 Bloqueo), DEBUFF_DEF (15 Corromper), DEBUFF_SPD (5 Interferir), HEAL_PCT (33 Backup), DRAIN_PCT (14 Devorar), RECOIL_PCT (8 Eco Doble), SELF_STUN (26 Pánico), CLEANSE (34 Depurar), SELF_DEBUFF_DEF (17 Gusano), DOT (13 Infección). Shape: 12 plain-damage rows, 8 damage-with-rider rows, 14 power-0 utility rows.

THE ACTUAL DEFECT IS NOT IN THE CONTENT OR THE ENGINE - IT IS THE OPPONENT. `battle_ai.cpp::expected_damage_half()` returns 0 for `power == 0` (`Errata/src/game/battle_ai.cpp:66`) and `battle_ai_move_score()` is exactly `expected_damage_half() * battle_accuracy_eff()` (`Errata/src/game/battle_ai.cpp:142`). The effect column is never read. Consequences, all three measurable:
 (a) all 14 power-0 rows score 0 and are chosen only when every damaging move is on cooldown - stated as a limit at `Errata/src/game/battle_ai.h:102-106`, asserted as correct by `tests/test_battle_ai.cpp:711`;
 (b) the 8 rider rows are priced as if the rider were free, so the AI OVER-picks the two worst-costed moves in the pack. Species 36 Murax's real kit is {20 Sobrecarga 75/85, 26 Pánico 95/70, 28 Apuesta 100/55, 31 Sandbox 0/100} (`Errata/src/data/species_table.h:158`). All four are SYSTEM/NEUTRAL against a fixed foe, `battle_damage_pre_roll` is monotone in `power`, so the ranking is power×accuracy: Pánico (self-stuns for a round) outranks Sobrecarga, and Sandbox scores 0;
 (c) `tests/tools/balance_matrix.cpp:16-22` already names this in its own banner: "battle_ai.cpp scores every power-0 move at 0 - so PROTECT_HALF, the three BUFF_* moves, CLEANSE and the DOT are chosen ONLY when every damaging move is on cooldown."

AND THE ROSTER WAS TUNED AGAINST A DIFFERENT, EFFECT-AWARE CHOOSER. `tools/content/sim_engine.py:105-155` (`score_move`) prices every one of the 16 effects in floats: RECOIL −0.9×ed×v/100, DRAIN +0.8×, DOT +0.55×v×dur, EFF_CORRUPT +0.45×best, SELF_DEBUFF_DEF −0.30×best, SELF_STUN −0.55×best, the three BUFFs at 0.55/0.50/0.35×best×v×min(dur,4)/3 gated on `stage < 2`, the three DEBUFFs at 0.45/0.50/0.30×best gated on `stage > −2`, PROTECT at 1.05×best below 55 % HP and 0.42×best above, HEAL at 0.85×actual-heal, CLEANSE at 0.9×best×(count of things to clean) or −1 when there is nothing. The win-rate matrix `verify.py` prints came from that chooser. The firmware ships the damage-only one.

PLAYER-VISIBLE HALF. The battle menu prints `a->power` as each row's right-hand value (`Errata/src/ui/screen_battle.cpp:1243`), so the 14 utility moves render as "Amplificar 0", "Firewall 0", "Backup 0", "Depurar 0" - the UI literally tells the player they do nothing. Every effect DOES get an event line once it resolves (`Errata/src/ui/screen_battle.cpp:502-514`, strings `STR_BT_PROTECT/DOT/CORRUPT/STUN/CLEANSE`), so the presentation of outcomes is fine; only the menu is silent. `tests/test_screens.cpp:3840-3847` records the same fact from the other side: species 1's learnset has no PROTECT move at all, "which is exactly why the effect went five phases without a picture."

TWO LIVE PROSE ERRORS, NEITHER CAUGHT BY ANY GATE. `docs/manual/content/guide.typ:933` "Protegerse *anula* el golpe de esa ronda" / `:944` "Protect *cancels* that round's hit" - the engine HALVES it (`PROTECT_DIVISOR 2`, `Errata/src/data/balance.h`, applied at `Errata/src/game/battle.cpp:623-626`). And the public wiki prints raw enum names lowercased, so a reader sees "eff corrupt" and "self debuff def" (`tools/wiki/page.html:748-750`).

BUDGET, MEASURED, NOT RECALLED. `release` at HEAD (0499ec8) is 1,363,996 flash / 61,276 globals against `GATE_RELEASE_FLASH_MAX 1600000` and `GATE_RELEASE_GLOBALS_MAX 65000` (`Errata/src/core/config.h:698-699`), i.e. **236,004 B of flash free and 3,724 B of globals free** (`CHANGELOG.md:308-310`). `docs/budget.md:1203` still says 59,468 / 5,532 free and is two commits stale - the exact defect that file's own §16 warns about.

## La costura
PRIMARY SEAM - `Errata/src/game/battle_ai.cpp`, two functions:
 * `expected_damage_half()` (`:63-101`) stays exactly as it is. It is the engine's own `battle_damage_pre_roll()` and must not learn about effects.
 * `battle_ai_move_score()` (`:103-143`) gains an effect term. Add one new file-scope `static uint32_t/int32_t ai_effect_bonus(const BattleCombatant& u, const BattleCombatant& f, const AttackDef& a, uint32_t ref)` beside it, a 16-arm switch on `a.effect` mirroring `tools/content/sim_engine.py:105-155` in integers, plus `static_assert((int)ATK_EFF_COUNT == 16, ...)` in the same form `Errata/src/game/battle.cpp:705` already uses.

SECONDARY (documentation of behaviour, same file pair): `Errata/src/game/battle_ai.h:102-106` - limit #1 currently asserts the behaviour being removed and must be rewritten, not deleted.

THIRD (the instrument): `tests/tools/balance_matrix.cpp:16-22` - its banner explains a caveat that stops being true.

TESTS: `tests/test_battle_ai.cpp` (1,242 lines, 24 TESTs). The one that inverts is `a_status_move_scores_zero_and_the_ai_still_answers_when_zero_is_all_there_is` (`:711-740`). Eight other cases call `battle_ai_move_score()` directly and pin relative orderings, not absolutes (`:543`, `:585`, `:617`, `:690`, `:456`) - those should survive if the NONE arm contributes exactly 0.

GOLDENS: `tests/golden/screens/battle_{hit,faint,protect,result,menu}.pbm` are captured from `BT_ENTRY_PRACTICE` battles (`tests/test_screens.cpp:3781, 3797, 3811, 3828, 3889, 3925`), i.e. the foe is AI-driven, so a different chooser moves them.

NOT the seam, deliberately: `tools/content/attacks.json`, `tools/content/species.json`, `Errata/src/data/attacks_table.h`, `Errata/src/game/battle.cpp`. No content byte and no engine rule changes.

## Pasos (del lector, POSIBLEMENTE MAL - ver correcciones)
1. Re-measure the floor first: run `tools/build_matrix.sh` alone and record the release flash/globals line. docs/budget.md:1203 is stale (59,468/5,532); CHANGELOG.md:308-310 says 1,363,996/61,276. Do not plan against either until one is confirmed at HEAD.
2. Decide the `ref` question BEFORE writing code (see blocking_unknowns): sim_engine.py's utility arms scale by `best` = the max expected damage over the user's OWN four moves, but `battle_ai_move_score(st, side, move_slot)` is a PUBLIC per-slot query (Errata/src/game/battle_ai.h:243) that eight cases in tests/test_battle_ai.cpp call directly. Either compute `best` inside move_score by looping the user's other three slots (keeps the signature, costs 4x the pre_roll work per call, ~16 calls per choose - still trivial), or add a private two-pass path in battle_ai_choose() and leave the public query damage-only (which would leave the public query lying). Recommend the first: keep the signature, loop the kit.
3. Write `ai_effect_bonus()` in battle_ai.cpp as a signed integer switch over all 16 ATK_EFF_* slots, in the same units the score already uses (half-points x accuracy-percent, u32, ceiling 89,400 per Errata/src/game/battle_ai.cpp:130-140, so a x100 numerator cannot overflow u32). Port the sim's coefficients as integer permille or /100 fractions - e.g. SELF_STUN -55*best/100, RECOIL -90*ed*v/10000, PROTECT (hp*100 < hp_max*55 ? 105 : 42)*best*dur/100. NONE returns exactly 0.
4. Carry the sim's two GATES, not just its weights: a BUFF_* arm scores nothing when `u.stage[k] >= BUFF_STAGE_MAX`, a DEBUFF_* arm scores nothing when `f.stage[k] <= BUFF_STAGE_MIN`, CLEANSE scores nothing when the user has no negative stage, no dot_left and no corrupt_left. These are what stop the AI spamming a capped buff forever, and they are the arms a mutation sweep must be able to break.
5. Add `static_assert((int)ATK_EFF_COUNT == 16, "AttackEffect grew: battle_ai.cpp::ai_effect_bonus prices 16 slots and a new one would be worth nothing")` directly under the switch. A 17th effect must now fail the build in TWO places, not one.
6. Add the effect term at Errata/src/game/battle_ai.cpp:142, clamped at 0 so a self-stun penalty can never make a legal move score below an illegal one's 0 and break the never-invalid contract that tests/test_battle_ai.cpp:301 and :347 police.
7. Rewrite Errata/src/game/battle_ai.h:102-106 (limit #1). It must now say what the chooser DOES price and what it still does not (no look-ahead; the effect weights are sim_engine.py's and were tuned in floats, so the integer port is an approximation of an approximation). Do not quote a win-rate: battle_ai.h:139-144 already forbids it.
8. Rewrite tests/test_battle_ai.cpp:711 into the named cases listed under `guards`. Each must fail when the arm it covers is deleted - run that mutation sweep, it is the house standard.
9. Re-run `tests/tools/balance_matrix` (`make -C tests balancetool`) and rewrite its banner at :16-22. Read the new matrix before touching any number in attacks.json - the previous matrix's cells were shaped by a chooser that could not use half the pack.
10. Re-record the screen goldens: `make -C tests capture` then `tools/pbm2svg.py --all`, then `tools/build_wiki.py` and `tools/build_manual.sh --draft`. Install `typst` first (docs/manual/README.md) or --verify silently skips the PDF and the gate lies - CLAUDE.md says this happened for two commits.
11. Fix the two stale comments found while mapping: tests/test_battle_golden.cpp:33-38 and :235-237 claim ATK_EFF_DOT is unreachable by any shipped learnset. It has been reachable since ROSTER_FAMILIES went to 20 (species 46 Glitchi). Free, and it is the same class of lie the commit is about.
12. Run the three gates one at a time, in order, never in parallel: `make -C tests check` (62 binaries), `tools/check.sh`, `tools/build_matrix.sh`.
13. Run the guide agent LAST, after the goldens are recorded: Agent(subagent_type: "guide"). It gets both artefacts - the booklet because the opponent's behaviour in combat changed and the captures moved, the web because docs/index.html is generated from those captures. Hand it the PROTECT prose error at docs/manual/content/guide.typ:933,944 explicitly; no gate will surface it.

## Guardas que dispararán
- make -C tests check - 62 host binaries, run alone. This is where every named test below lives.
- NEW tests/test_battle_ai.cpp :: the_ai_prefers_sobrecarga_to_panico_because_the_self_stun_is_priced - arm species 36 Murax's real kit {20,26,28,31} (Errata/src/data/species_table.h:158). TODAY the AI picks 26 Pánico (power 95 x acc 70 beats 75 x 85). Delete the SELF_STUN arm and this fails by name. This is the single case that proves the commit did the thing it claims.
- NEW :: a_recoil_move_is_worth_less_than_the_same_power_without_it - two arms the sim charges (RECOIL_PCT, SELF_DEBUFF_DEF); deleting either loses one.
- NEW :: a_buff_is_not_taken_again_once_its_stage_is_capped - set u.stage[BSTAT_ATK] = BUFF_STAGE_MAX; the BUFF_ATK score must fall back to ~0. Delete the cap gate and the AI spams Amplificar forever; this fails.
- NEW :: a_debuff_is_not_taken_again_once_the_foe_is_floored - the mirror gate at BUFF_STAGE_MIN.
- NEW :: protection_is_worth_more_below_half_health_than_above_it - pins the sim's 1.05/0.42 split at the hp_max*55/100 boundary, both sides, strictly, the way tests/test_battle_ai.cpp:761 already pins the 25 % switch threshold.
- NEW :: cleanse_is_worth_nothing_with_nothing_to_clean_and_something_with_a_dot_running - delete the `bad` count and the AI cleanses a clean Bug; fails.
- NEW :: every_ATK_EFF_NONE_row_scores_exactly_what_it_scored_before - a literal pin on two of the 12 plain rows, so the effect term provably cannot perturb plain damage.
- NEW static_assert in battle_ai.cpp: (int)ATK_EFF_COUNT == 16 - a 17th effect slot now breaks the BUILD in battle_ai.cpp as well as battle.cpp:705.
- KEPT UNCHANGED, and they are the proof the contract held: tests/test_battle_ai.cpp:301 every_action_the_ai_produces_over_sixty_four_battles_is_accepted_by_the_validator; :265 one_choice_costs_at_most_one_draw_from_the_ais_own_stream (a two-pass scorer must not draw more); :211 choosing_does_not_move_one_byte_of_the_battle_state; :1145 an_ai_driven_battle_replays_exactly_through_an_engine_that_never_runs_the_ai; :347 the_ai_answers_nothing_only_when_the_sweep_agrees_that_nothing_is_legal.
- tests/test_battle.cpp - all 66 cases must pass BIT-IDENTICAL. The engine is not touched; if one moves, the commit has leaked into battle.cpp.
- tests/test_battle_golden.cpp - tests/golden/battle_v1.txt is an AI-FREE script (tests/test_battle_golden.cpp:74-76) and must NOT move. If it does, BATTLE_ENGINE_VER or CONTENT_VERSION changed and the commit is not what it says it is.
- tests/test_screens.cpp - the five BT_ENTRY_PRACTICE captures WILL move (the foe is AI-driven) and must be re-recorded deliberately, with the diff read.
- tools/check.sh (run alone): layer purity - src/game must include no Arduino.h/u8g2/gfx.h/render.h (:632-634); battle* must draw from no named RNG stream (:645-650); the nine steps in order (:661-666); battle_ai must never take a mutable BattleState& (:682-687); tools/content/verify.py --fast (:160); gen_content.py --check, 8 files in sync (:162); gen_content.py --selftest (:172); the balance.h <-> balance.json tuning-constant parity gate (:474-500); UTF-8 over the whole tree (:1618/:2764); pbm2svg.py --check, no manual SVG behind its golden (:2675/:3821); the capture-set-vs-golden-set cross check (:3850); build_wiki.py --check, docs/index.html vs roster+sprites+captures (:1567/:2713); build_manual.sh --draft --verify, docs/uso.pdf vs its sources (:3900) - which needs typst installed or it skips the PDF in silence.
- tools/build_matrix.sh (run alone): 6 variants, zero warnings, and the two hard caps - release flash <= 1,600,000 and release globals <= 65,000 (tools/build_matrix.sh:76-92). The globals line is the one to watch: 61,276 today, 3,724 B of room.
- tests/tools/balance_matrix (make -C tests balancetool, not part of check) - re-run and read. Its --self-check side-0 symmetry band must still hold.
- THE GUIDE AGENT (.claude/agents/guide.md), last, after the goldens - CLAUDE.md's rule fires on all of: a mechanic that changes rules, goldens in tests/golden/screens/ that move, and docs/index.html regenerating from them.

## Riesgos
- The balance matrix will move, possibly a lot. The roster's 40-63 % band came from sim_engine.py's effect-aware chooser (tools/content/verify.py) and the firmware matrix came from the damage-only one; making the firmware match the sim moves the FIRMWARE numbers toward the tuned ones, which is the point, but species whose kits are built around a status move will rise and the previously-dominant RISK users (26 Pánico, 17 Gusano, 8 Eco Doble) will fall. Read the new matrix; do NOT retune attacks.json in the same commit on the strength of it.
- The integer port is not the float sim. sim_engine.py works in doubles; src/game forbids floating point. Rounding will reorder some near-ties. This must be stated in battle_ai.h as a measured limit, in the same form as the existing protected-defender inversion note (battle_ai.h:114-133), not glossed.
- battle_ai_move_score() is public and eight cases pin relative orderings against it. If the utility arms are scaled by the kit's `best`, the function's answer starts depending on the user's other three move slots - a real semantic widening of a public query. It is defensible (the header's own doc already says it ranks WITHIN a kit) but it must be written into the header, not discovered later.
- Screen goldens moving is a cascade, not a detail: battle_{hit,faint,protect,result}.pbm -> docs/manual/assets/screens/*.svg -> docs/uso.pdf -> docs/index.html. Four gates and the guide agent. Budget the commit for it.
- The AI getting BETTER is a product change, not only a technical one. A practice opponent that protects, buffs and cleanses is harder; the manual's combat page (docs/manual/content/guide.typ:920-950) currently describes a fight the player wins by hitting things. That is the guide agent's judgement call and it needs the context.
- Deleting the `battle_ai_move_score == 0` pin at tests/test_battle_ai.cpp:711 removes a real guard. It must be REPLACED by the named cases above, not merely dropped - the house rule is that a test you cannot break was not protecting anything.

## Incógnitas bloqueantes
- Do the five BT_ENTRY_PRACTICE screen goldens actually move? Near-certain (the foe is AI-driven) but NOT measured here - I did not build. `make -C tests capture` before scoping the commit; if they move, the guide agent and four doc gates are in scope and the commit is roughly twice the size.
- The `best` / `ref` decision named in mechanical_steps step 2. It determines whether battle_ai_move_score()'s public contract widens and whether eight existing test cases need re-pinning. Decide before writing a line.
- Does `const uint8_t[16]` land in .rodata (flash) or .dram0.rodata (globals) on this Arduino core? Only tools/build_matrix.sh answers it. Worst case is 16 B of 3,724; sidestep it by writing literals into the switch arms.
- The exact integer scale factor for the effect term. The existing score is expected_damage_half * accuracy_eff, u32, measured max 34,170 on the roster and arithmetic ceiling 89,400 (Errata/src/game/battle_ai.cpp:130-140). A x100 fixed-point numerator gives 8.94M headroom against 4.29e9 - safe - but the SIGNED intermediate for the penalty arms must be worked in int32_t or int64_t and clamped to 0 before the u32 return, and that needs to be written down, not assumed.
- Whether the project wants an EFFECT INDICATOR in the move menu at all (Errata/src/ui/screen_battle.cpp:1243 prints `a->power`, so 14 moves show "0"). BT_VAL_CAP is 7 (screen_battle.cpp:66) so there is room for a short tag, but it needs 16 Spanish labels and a design decision. I am cutting it; someone has to agree that is the right cut.

## Alcance recomendado
THE ONE COMMIT: **"battle: the opponent plays the moves the pack already has."**

Not "more effects". The map says the enum is complete (16/16 implemented, `battle.cpp:644-707`), the content is complete (34/34 attacks on a learnset, `test_content.cpp:533`), and every effect has a named engine test among test_battle.cpp's 66. What is missing is that the only opponent a solo player ever fights cannot see the effect column at all (`battle_ai.cpp:66,142`), so 22 of 34 rows - every utility move and every rider - are either invisible to it or mispriced, and it actively prefers the two self-harming RISK moves because their downside is free. That is what "attacks that actually do something" means here, and it is fixable without moving one byte of content.

WHAT SHIPS
1. `battle_ai.cpp`: `ai_effect_bonus()`, a 16-arm integer port of `sim_engine.py:105-155` - the SAME weights the 60-species roster was tuned against - with the sim's two gates (buff capped, nothing to cleanse). `+static_assert((int)ATK_EFF_COUNT == 16)`.
2. `battle_ai.h:102-106` rewritten: limit #1 currently asserts the behaviour being deleted.
3. `tests/test_battle_ai.cpp`: :711 replaced by the eight named cases in `guards`, each verified to fail when its arm is deleted.
4. The five practice-battle screen goldens re-recorded, SVGs regenerated, `docs/index.html` and `docs/uso.pdf` rebuilt.
5. `tests/tools/balance_matrix.cpp:16-22` banner rewritten (it explains a caveat that stops being true) and the matrix re-run and read.
6. Free, found while mapping: `tests/test_battle_golden.cpp:33-38` and `:235-237` claim ATK_EFF_DOT is unreachable by any shipped learnset. It has been reachable since `ROSTER_FAMILIES` went to 20.
7. The guide agent, last, with the PROTECT prose error handed to it by line number.

Cost: ~1.4 KB flash of 236,004 free; **0 B of globals of 3,724 free**. Nothing else in this space is that cheap on the scarce axis.

WHAT I CUT, EXPLICITLY
* **Every content byte.** No new attack row, no re-assigned effect on the 12 NONE rows, no second DOT. That freezes CONTENT_VERSION, the recomputed `budget_cost` formula (`verify.py:204-225`), the per-stage learnset budgets (`:240-245`), `tests/golden/battle_v1.txt` (AI-free script), the `battle_dot_attack_count() <= 1` static_assert (`battle.h:699`), and `BATTLE_ENGINE_VER`. The 12 NONE rows are the plain chip/heavy ladder every kit needs - they are not the defect, and turning them into riders would be a retune, which is its own commit with its own matrix.
* **The move-menu effect indicator** (`screen_battle.cpp:1243`, where 14 moves show "0"). Real, and the obvious follow-up - but it needs 16 Spanish strings, moves `battle_menu.pbm`, and is where the guide agent's substantive writing is. Own commit.
* **A 17th effect kind.** Nine of sixteen are used exactly once; the pack is thin, not narrow. Widening the enum before the AI can use the sixteen it has is building on a floor nobody stands on.
* **The manual's "Protect cancels that round's hit"** (`guide.typ:933,944`, against `PROTECT_DIVISOR 2`) and the wiki's raw `eff corrupt` / `self debuff def` labels (`tools/wiki/page.html:750`). Both real, both prose, both handed to the guide agent as findings rather than fixed inline by me.
* **Retuning anything on the strength of the new matrix.** Read it in this commit; move numbers in the next one.

FEASIBLE NOW, with one caveat: the `best`/`ref` decision (blocking unknown 2) must be settled before the first line, and `make -C tests capture` should be run first to confirm the golden cascade is in scope.

## Coste: globals 0 B, flash 1400 B
ZERO, and here is the derivation rather than a guess. (1) `tools/build.sh:78` defines "globals" as arduino-cli's "Global variables use N bytes", which is DRAM only: .data + .bss. (2) `Errata/src/game/battle_ai.h:163` records, measured with `riscv32-esp-elf-size -A`, that `game/battle_ai.cpp.o` is 1,148 B of .text and "Neither file has any .data or .bss." (3) `tools/check.sh:682-687` forbids battle_ai from ever taking a mutable `BattleState&`, and battle_ai.cpp:5-8 states it has no file-scope mutable variable at all - so every byte the new scorer touches is stack or register. (4) A `const`/`constexpr` weight table is .rodata, which on this target is flash, not DRAM: the proof in this tree is P8-C4, which added a 41,658 B `.rodata` blob for +0 B of globals (CHANGELOG.md, docs/budget.md:151). (5) I propose NO new screen, NO new buffer, NO new persisted field, and no change to `BattleCombatant` (which is 32 B with exactly one reserved byte at offset 31, `Errata/src/game/battle.h:317-319`) - so `static BattleState s_st` (212 B, `Errata/src/ui/screen_battle.cpp:80`) does not move either.

HONEST UPPER BOUND if (4) is wrong on this Arduino core and a const table lands in `.dram0.rodata`: 16 bytes for a `uint8_t[ATK_EFF_COUNT]`. That is 0.43 % of the 3,724 B free. It is cheap enough that the table can simply be written as inline literal cases in the switch instead, which forecloses the question entirely - and that is what I would do.

The 3,724 B figure itself: 65,000 (`GATE_RELEASE_GLOBALS_MAX`, `Errata/src/core/config.h:699`) minus 61,276 measured (`CHANGELOG.md:308-310`).


# EL ESCÉPTICO

## Afirmaciones REFUTADAS
- "Eight other cases call battle_ai_move_score() directly and pin relative orderings, not absolutes (:543, :585, :617, :690, :456)" - FALSE. Those cases pin EXACT INTEGERS: tests/test_battle_ai.cpp:572 `CHECK_EQ(battle_ai_move_score(st, 0u, m), want[t][m])` against a literal {2600,2200,1800} table; :600-608 `CHECK_EQ(..., 2600u)/(2200u)`; :659-661, :670-671 `CHECK_EQ(..., 1200u)/(1312u)`; :682-683; :699-704 `CHECK_EQ(..., 1300u)`. They survive an added effect term only because every move they use (MV_PING=1, MV_BYTAZO=9, MV_ESCANEO=18, MV_CHOQUE=27, MV_APUESTA=28) has effect NONE - a different and much narrower argument than the one the plan makes.
- "tests/test_battle_ai.cpp (1,242 lines, 24 TESTs)" - 1,242 lines is right, but `grep -c '^TEST('` returns 25, not 24.
- "tests/test_battle_golden.cpp:33-38 ... (\"It CANNOT cover ATK_EFF_DOT... species 46, outside the shipped 36-species prefix\")" is listed as a live falsehood to fix. Half wrong. The operative sentence is still TRUE: the golden's teams are `G_A[3] = {9, 14, 6}` and `G_B[3] = {22, 27, 36}` (tests/test_battle_golden.cpp:39-40), and none of those six learnsets carries attack 13, so that recording genuinely cannot cover ATK_EFF_DOT. What is stale is only the REASON ("outside the shipped 36-species prefix" and "test_content.cpp already pins that by name"). :235-236 ("ATK_EFF_DOT, which no shipped learnset can reach") IS now false. Deleting the :33-38 sentence rather than its justification would make the file wrong in the other direction.
- "All four are SYSTEM/NEUTRAL against a fixed foe, `battle_damage_pre_roll` is monotone in `power`, so the ranking is power x accuracy: Panico (self-stuns for a round) outranks Sobrecarga" - the inference does not follow, and the conclusion is unmeasured. The score is `(2*pre_roll + DMG_RNG_SPAN - 1) * acc_eff` with DMG_RNG_SPAN = 3 (balance.h:401), i.e. `(2*pr + 2) * acc` - the additive +2 systematically favours the LOWER-power move. Concretely, at pre_roll 4 (Sobrecarga) vs 5 (Panico) the scores are 10*85 = 850 vs 12*70 = 840, so Sobrecarga already wins TODAY. The inversion only appears once pre_roll(75) is above roughly 4. The plan calls this "the single case that proves the commit did the thing it claims"; its premise depends on the atk/def/level the test arms and was never computed.
- The sim_engine.py port list is incomplete. It omits two arms that are exactly the "rider" cases the commit is about: `if e == "DEBUFF_SPD" and mv["power"] > 0: sc += best * 0.12` (tools/content/sim_engine.py:127-128) and `if e == "DEBUFF_ATK" and mv["power"] > 0: sc += best * 0.22` (:129-130). Attacks 5 Interferir (35 power, DEBUFF_SPD) and 21 Bloqueo (30 power, DEBUFF_ATK) are the only two power>0 debuffs in the pack, so a flat 16-arm switch on `a.effect` CANNOT express the sim: DEBUFF_ATK/DEBUFF_SPD each need a power test inside the arm, with different coefficients on each side of it.
- "PROTECT at 1.05x best below 55 % HP and 0.42x best above" - sim_engine.py:146 is `sc = best * (1.05 if ... else 0.42) * dur`; the `* dur` is dropped in exists_today (it does appear in mechanical_steps). Related: the sim returns **-1**, not 0, for a capped buff (:134-138), a floored debuff (:140-144) and a pointless cleanse (:152). A u32 score clamped at 0 cannot express "worse than nothing", so the proposed guard "the BUFF_ATK score must fall back to ~0" does not prove what it claims - a 0-scoring capped buff ties with every other 0 and battle_ai.cpp:287's tie-break can still hand it over.
- "a x100 numerator cannot overflow u32 ... A x100 fixed-point numerator gives 8.94M headroom" - the 89,400 ceiling (battle_ai.cpp:130-140) bounds the DAMAGE term only. A PROTECT arm at 105*best*dur/100 with dur up to 4 is ~4.2x best, i.e. ~37.5M at x100 fixed point, not 8.94M. Still comfortably inside u32, but the stated headroom is wrong by 4x.
- Line-number drift in three check.sh citations: the nine-steps-in-order gate is tools/check.sh:658-663 (plan says :661-666) and the mutable-BattleState gate is :679-683 (plan says :682-687). Also worth knowing: tools/check.sh contains the manual/wiki/UTF-8 gate blocks TWICE (2655-2795 and 3801-3936); the plan cites one copy of each.
- "The five BT_ENTRY_PRACTICE captures WILL move" - at most four can. battle_menu is snapshotted at tests/test_screens.cpp:3817, immediately after `battle_input(GST_TAP_R)` skips the stare-down and BEFORE any action is submitted, so no AI choice has been made yet; battle_pick (:3787) and battle_intro (:3801) are earlier still. Only battle_hit / battle_protect / battle_faint / battle_result can move.

## Guardas que el lector NO vio
- THREE AI-DRIVEN TEST BINARIES THE PLAN NEVER NAMES. `grep -rn battle_ai_choose` finds consumers at tests/test_session.cpp:172, tests/test_link_transport.cpp:468 and tests/test_link_screen.cpp:354 - all three run whole battles with the chooser on one or both sides. Assertions that can move: test_link_transport.cpp:552 `CHECK(T.lk.burst_max > 7u)` and :553 `CHECK(T.lk.burst_max < LINK_RX_SLOTS)`; test_link_transport.cpp:538 `CHECK(session_end(...).round >= 1u)`; test_session.cpp:319 `CHECK(T.e[0].st.round >= 2u)` and :321 `CHECK_EQ(T.e[0].blog.dropped, 0)`; test_session.cpp:1702-1712 (walks to a named round, then compares `session_end().round` against `died_at`); test_link_screen.cpp:1064 `CHECK(g_peer_moves > 0)`.
- BATTLE_MAX_ROUNDS = 60 (Errata/src/data/balance.h:560, with CORRUPT_BATTLE_ROUNDS static_asserted equal to it at battle.h:206). An AI that protects (31 Sandbox, dur 2), heals (33 Backup, 25 % of hp_max, cd 4) and cleanses makes battles strictly longer - on BOTH sides in the AI-vs-AI loopback trials. Battles that ended on a faint may now end on the round cap, which changes who won and therefore every reward/XP arm downstream in test_session.cpp. Nothing in the plan bounds this.
- `#define BATTLE_AI_VER 1u` (Errata/src/game/battle_ai.h:192): "Bumped by any commit that changes which action this chooser returns." The header itself says no code enforces it - which is precisely why a plan has to carry it. It is absent from the plan.
- THE GOLDEN RE-RECORD STEP IS THE WRONG TARGET. Plan step 10 says `make -C tests capture`; tests/Makefile:797-800 shows that rebuilds only the REAL-FONT set under tests/capture/golden/screens. The fake-font goldens under tests/golden/screens/*.pbm - the ones test_screens compares against and the ones pbm2svg.py reads - are re-recorded by `make -C tests screens` -> `./bin/test_screens --record` (tests/Makefile:1029-1030). Following the plan literally leaves `make -C tests check` red and pbm2svg.py --check comparing against stale goldens. And do NOT reach for `make -C tests golden` instead: that target is `test_care_golden --record` + `test_battle_golden --record` (tests/Makefile:1023-1025) and would silently re-record tests/golden/battle_v1.txt, destroying the plan's own "battle_v1.txt must not move" guard.
- tools/check.sh:3845-3855 cross-checks the two golden SETS by filename and fails by name if one set has screens the other does not - so the two re-record commands are a pair, not a choice.
- THE `best` LOOP HAS TWO UNSPECIFIED SEMANTICS. sim_engine.py:112 computes `best` over ALL of `user.moves`, including moves currently on cooldown (only the candidate itself is rejected, at :109). A port that skips cooldowned moves diverges from the sim on exactly the turns where utility moves get chosen. And the loop must skip `moves[i] == 0` (attack_get(0) is nullptr, battle_ai.cpp:128) or it will price a kit of one move as if `best` were 0 and make every utility arm worth nothing.
- A UNITS TRAP THE x100 SCALE DOES NOT COVER. sim_engine.py's DOT arm (`sc += v * dur * 0.55`, :120) and HEAL arm (`sc = heal * 0.85`, :148-149) are in RAW HP; every other arm is a multiple of `best`. The firmware's score is 2*damage*accuracy, i.e. roughly 200x the sim's `ed`. The two HP-denominated arms therefore need the 2x AND the accuracy factor as well as the x100, and the plan's single "x100 fixed-point numerator" sentence does not say so. Off by 100x, Backup and Infeccion become either never-chosen or always-chosen and no test in the plan's list would catch it.
- NOTHING WILL EVER CHECK THAT THE PORTED WEIGHTS STILL MATCH sim_engine.py. tools/check.sh:474-509 is a FIXED scalar map (K, PROTECT_DIVISOR, BUFF_STAGE_MIN/MAX, EVASION_*, ACCURACY_MIN, ...); new AI coefficients in battle_ai.cpp do not appear in it and cannot be added without editing that map. The commit creates a firmware/simulator coupling with no gate - the exact defect class CLAUDE.md names - and the plan does not mention it.
- tests/test_content.cpp:543-553 `every_attack_category_and_effect_has_at_least_one_row` is what keeps "16 effects, all used" true and is what makes the 16-arm switch total. Not at risk, but it belongs in the guards list as the reason the switch is exhaustive.

## Veredicto de coste
GLOBALS: the 0 B claim is DEFENSIBLE and the derivation holds. I re-derived it independently. (1) Errata/src/game/battle_ai.h:163 states, measured with `riscv32-esp-elf-size -A`, "Neither file has any .data or .bss" - verified as the file's own text. (2) battle_ai.cpp:5-8 forbids any file-scope mutable variable and tools/check.sh:679-683 enforces the const-BattleState half of that rule, so every byte the new scorer touches is stack or register. (3) A const weight table is .rodata, which on ESP32-C3 is flash-mapped (drom) and is not counted by arduino-cli's "Global variables use N bytes"; the plan's own fallback (write the coefficients as literals in the switch arms) forecloses the question entirely, and that is the right call. (4) Nothing in the plan widens BattleCombatant (verified 32 B with one reserved byte at offset 31, battle.h:317-319 and the three static_asserts at :319-329) or `static BattleState s_st` (212 B, screen_battle.cpp:80). I concur: 0 B of globals.

The 3,724 B free figure is also right: GATE_RELEASE_GLOBALS_MAX 65000 (config.h:699, verified exact) minus 61,276 (CHANGELOG.md:308-310, verified exact, and that Cost block belongs to the top-most entry, i.e. HEAD 0499ec8). docs/budget.md:1203 does say 59,468 / 5,532 and is indeed stale.

FLASH: 1,400 B is an unmeasured upper bound rather than an estimate, and the plan should say so. battle_ai.cpp.o is 1,148 B of .text today (battle_ai.h:156), so +1,400 B means more than doubling the file. My own number: a 16-arm integer switch plus a four-slot `best` loop is realistically +500 to +1,000 B of .text on riscv32 at -Os; 1,400 B is a safe ceiling. Either way it is under 0.6 % of the 236,004 B free, so flash is not the binding axis and the plan is right to say so. The one thing that IS worth measuring is not the code size at all: it is whether making the chooser more expensive (16 extra battle_damage_pre_roll calls per choose) matters on the device - nothing in the plan measures the per-choose cost, and screen_battle.cpp calls battle_ai_choose() inside the frame path at :689.

## Veredicto de alcance
ONE COMMIT, but not the one the plan scoped, and NOT achievable without breaking a gate as written.

Three reasons it is not achievable as written:
1. Step 10 re-records the wrong golden set (`make -C tests capture` instead of `make -C tests screens`), so `make -C tests check` and tools/check.sh:3821 would both be red at the end of the sequence.
2. Three test binaries that run AI-driven battles (test_session, test_link_transport, test_link_screen) are outside the plan's scope and outside its guard list. They are part of `make -C tests check`, so they gate the commit whether the plan names them or not.
3. The flagship test's premise (the AI prefers Panico to Sobrecarga today) is arithmetically not established and may be false for the state the test arms.

With those fixed it IS one commit, and a defensible one: the code surface is genuinely two functions in one file plus one header paragraph, no content byte moves, CONTENT_VERSION and BATTLE_ENGINE_VER stay frozen, tests/golden/battle_v1.txt stays frozen (verified AI-free: tests/test_battle_golden.cpp:74-77 scripts both sides). The doc cascade is real but smaller than the plan fears - at most four screen goldens, not five.

What genuinely should be a SEPARATE commit, and the plan already cuts it correctly: the move-menu effect indicator (screen_battle.cpp:1243 with BT_VAL_CAP 7, both verified). The two prose fixes (guide.typ:933/944 "anula"/"cancels" against PROTECT_DIVISOR 2, and tools/wiki/page.html:750's `a.effect.toLowerCase().replace(/_/g," ")` printing "eff corrupt") are verified real and correctly handed to the guide agent rather than fixed inline.

## PASOS CORREGIDOS (usa ESTOS)
1. MEASURE THE GOLDEN CASCADE FIRST, and do it cheaply. `make -C tests bin/test_screens && ./tests/bin/test_screens` at HEAD to confirm green, then after the code change re-run it and read which .pbm the diff touches. build_foe() rolls the practice foe over the WHOLE table - `sp = 1 + (h % SPECIES_TABLE_COUNT)`, screen_battle.cpp:309, SPECIES_TABLE_COUNT = 60 - so the foe's kit is arbitrary and the hit/protect/faint/result captures probably do move. battle_pick, battle_intro and battle_menu are snapshotted before any action is submitted (test_screens.cpp:3787, :3801, :3817) and CANNOT move.
2. Re-measure the floor: run tools/build_matrix.sh alone, record release flash/globals. CHANGELOG.md:308-310 (1,363,996 / 61,276) is at HEAD; docs/budget.md:1203 (1,354,812 / 59,468) is stale.
3. Settle the `best`/`ref` question, and settle THREE sub-questions with it, not one: (a) public signature vs private two-pass; (b) does `best` include moves on cooldown (sim_engine.py:112 says yes - only the candidate is rejected, at :109); (c) skip moves[i] == 0 in the loop, because attack_get(0) is nullptr.
4. Settle the SCALE before writing an arm. The score is 2*damage*accuracy. sim_engine.py's `best`-relative arms port as fractions of the existing score; but the DOT arm (v*dur*0.55, :120) and the HEAL arm (heal*0.85, :148) are in RAW HP and need the 2x and the accuracy factor applied explicitly. Write the chosen scale down in a comment before the switch.
5. Write ai_effect_bonus() as a SIGNED int32 switch, then clamp to 0 on return. It is not a flat 16-arm switch: DEBUFF_ATK and DEBUFF_SPD each need a power>0 branch (sim_engine.py:127-130, +0.22 and +0.12 of best as riders) distinct from their power==0 branch (:139-144, 0.45 and 0.30 of best, gated on foe stage > BUFF_STAGE_MIN). PROTECT carries * dur (:146). NONE returns exactly 0.
6. Carry the sim's three gates: buff arms dead at stage >= BUFF_STAGE_MAX, debuff arms dead at foe stage <= BUFF_STAGE_MIN, CLEANSE dead with no negative stage, no dot_left and no corrupt_left. Note in the header that the sim scores these -1 and the firmware can only reach 0, so a capped buff TIES with a 0-scoring move instead of losing to it - and write the tie case into the test rather than pretending 0 is the same answer.
7. Add `static_assert((int)ATK_EFF_COUNT == 16, ...)` under the switch, in battle.cpp:705's form.
8. Bump `#define BATTLE_AI_VER` from 1u to 2u (Errata/src/game/battle_ai.h:192). Its own comment says any commit that changes which action the chooser returns must bump it, and no code enforces it.
9. Rewrite battle_ai.h:102-106 (limit #1), and add a SIXTH limit: the weights are sim_engine.py's, ported to integers, and nothing gates the two against each other - tools/check.sh:474-509's balance parity map is a fixed scalar list that new AI coefficients cannot join. Say so in the header, in the form battle_ai.h:114-133 already uses.
10. Rewrite tests/test_battle_ai.cpp:711 into the named cases. BEFORE writing the Murax case, COMPUTE its premise: the score is (2*pre_roll + 2) * acc_eff, so at pre_roll 4 vs 5 Sobrecarga already wins today (10*85 = 850 > 12*70 = 840). Pick an atk/def/level where the inversion is real, or the case proves nothing. Keep the existing absolute pins at :572, :600-613, :659-671, :682-683, :699-704 UNCHANGED - they use only effect-NONE moves and are the proof the NONE arm contributes exactly 0.
11. Run the three AI-driven loopback binaries deliberately and read them, not just their pass/fail: tests/test_session.cpp, tests/test_link_transport.cpp, tests/test_link_screen.cpp. Watch test_link_transport.cpp:552-553 (burst_max band), test_session.cpp:319-321, and any trial that now runs to BATTLE_MAX_ROUNDS = 60 (balance.h:560) instead of ending on a faint.
12. Re-run tests/tools/balance_matrix (`make -C tests balancetool`), read it, rewrite the banner at tests/tools/balance_matrix.cpp:15-22. Change no number in attacks.json.
13. Re-record BOTH golden sets, in this order: `make -C tests screens` (that is ./bin/test_screens --record, tests/Makefile:1029-1030) for tests/golden/screens/*.pbm, then `make -C tests capture` for tests/capture/golden/screens, then `tools/pbm2svg.py --all`. Do NOT run `make -C tests golden` - that target is test_care_golden/test_battle_golden --record (tests/Makefile:1023-1025) and would re-record tests/golden/battle_v1.txt, which must not move. Confirm with `git diff --stat tests/golden/battle_v1.txt` that it is untouched.
14. Install typst, then tools/build_wiki.py and tools/build_manual.sh --draft.
15. Fix the stale audit trail precisely: tests/test_battle_golden.cpp:235-236 ("ATK_EFF_DOT, which no shipped learnset can reach") is now FALSE and must go. tests/test_battle_golden.cpp:33-38's "It CANNOT cover ATK_EFF_DOT" is still TRUE for this recording (G_A {9,14,6}, G_B {22,27,36}, none of which knows attack 13) - replace only its justification, which claims a 36-species prefix and a test_content.cpp pin that no longer exist.
16. Run the three gates one at a time: make -C tests check (62 binaries), tools/check.sh, tools/build_matrix.sh.
17. Guide agent last, with: the PROTECT prose error (docs/manual/content/guide.typ:933 "anula" / :944 "cancels" against PROTECT_DIVISOR 2, applied at battle.cpp:623-626), the wiki's raw enum labels (tools/wiki/page.html:750), and the fact that the practice opponent now plays status moves - which is a change to the fight the combat page at guide.typ:920-948 describes.