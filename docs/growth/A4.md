# RECON A4

## Lo que ya existe
THE ROSTER IS ALREADY AT THE PACK'S CEILING; `ROSTER_FAMILIES` IS NO LONGER A CLAMP.

`ROSTER_FAMILIES = 20` (tools/gen_content.py:73) and `tools/content/species.json` holds exactly 60 rows. The generator emits a PREFIX of the pack (`self.species = [s for s in self.species_all if s["family"] <= families]`, tools/gen_content.py:110), so at 20 the prefix IS the pack. `SPECIES_TABLE_COUNT == SPECIES_PACK_COUNT == 60` and `SPECIES_FAMILY_COUNT == SPECIES_PACK_FAMILY_COUNT == 20` (Errata/src/data/species_table.h:210-228), and both are DERIVED by the generator from the JSON (tools/gen_content.py:860, :875-876). **Raising `ROSTER_FAMILIES` to 21 today emits nothing new — there is no family 21 in the JSON.** The growth item is therefore "author a 21st family", not "raise a constant". docs/content.md:151-165 lists the package deal and OMITS the JSON authoring step entirely (it was written when the pack was ahead of the ship); that doc is stale in exactly the way the house rule warns about.

THE HARD CEILING IS 66 FAMILIES / 198 SPECIES, AND IT IS THE CREATOR ID SPLIT. `SPECIES_ID_BUILTIN_MAX 199u // 200..209 are the creator's cs0..cs9` (Errata/src/data/species_table.h:62), asserted by `static_assert(SPECIES_TABLE_COUNT <= SPECIES_ID_BUILTIN_MAX, "roster exceeds id 199")` (species_table.h:313) and enforced on the wire at networking/protocol.cpp:131. 198 = 66 x 3 is the largest multiple of 3 under it. `CREATOR_SPECIES_ID_MIN = SPECIES_ID_BUILTIN_MAX + 1` (Errata/src/data/creator_schema.h:65), so the ceiling cannot be raised without moving every custom species id.

THE THREE REAL BINDING CONSTRAINTS, MEASURED:

1. THE DEX BIT ARRAY BINDS FIRST, AT 61 SPECIES. `#define DEX_BYTES 15` (Errata/src/core/nt_types.h:425) = 120 bits = exactly 60 species at 2 bits each, and `static_assert((int)SPECIES_TABLE_COUNT * DEX_BITS_PER_SPECIES <= DEX_BYTES * 8, ...)` (Errata/src/game/dex.h:48-51) fires on species 61. This is the first guard a 21st family trips. It is a ONE-BYTE fix with ZERO globals cost: `uint8_t dex[DEX_BYTES]; uint8_t reserved[152 - DEX_BYTES];` (Errata/src/persistence/save_schema.h:313-314) inside a struct pinned at `static_assert(sizeof(ConfigV2) == 256, ...)` (save_schema.h:318). `reserved` is required-zero and `offsetof(ConfigV2, dex) == 102` is asserted (save_schema.h:321), so growing dex[] eats reserved[] in place, no `SAVE_SCHEMA_VERSION` bump, and an older blob still reads zeros = "nothing discovered yet" (the reasoning is written out at save_schema.h:306-312). CRITICALLY, this is safe here and was NOT safe in `Config`: `Config.reserved_b[24]` carries real v1 fixture data ("41.3874"/"2.1686", asserted by name at tests/test_fixtures.cpp:102-104) — ConfigV2 is BUILT by the migration, not mapped from v1 bytes (nt_types.h:442-449). DEX_BYTES 16 covers 64 species; 66 families/198 species would need 50 bytes, still inside the 152.

2. THE ART BUDGET HAS ROOM FOR EXACTLY SEVEN MORE SETS — ENOUGH FOR TWO FAMILIES, NOT THREE. Bug atlas: `ER_SPRITE_DATA_BYTES_DECLARED 9216u` against `ER_SPRITE_DATA_BYTES_MAX 10240u` (Errata/src/data/sprites_bugs.h, generated; the ceiling is `ER_DATA_BYTES_MAX = 10240` at tools/gen_sprites.py:95) = 1,024 B = 7 sets. Aggregate: `SPRITE_DATA_BYTES_DECLARED 10247u` / `SPRITE_DATA_BYTES_MAX 11264u` (Errata/src/data/sprites.h:395-396) = 1,017 B. One family is 3 x 144 = 432 B: 9,648 <= 10,240 and 10,679 <= 11,264. **So the gen_content.py:62 banner and docs/content.md:160-165 are WRONG that one more family needs `SPRITE_DATA_BYTES_MAX` raised — it does not.** Two families (864 B) also fit (10,080 and 11,111). THREE families do not (10,512 > 10,240). What DOES have to move for one family is the two DECLARED literals and the pinned numbers in tests/test_sprite_pipeline.cpp:599-639.

3. GLOBALS ARE NOT SPENT AT ALL BY THIS FEATURE. Every table involved is `inline constexpr` — `SPECIES_TABLE` (species_table.h), `EVOLUTION_RULES` (evolution_table.h:54), `ER_SPRITE_SETS` / `ER_SPRITE_EYES` / `bug_spr_*` (sprites_bugs.h:2172, :2262) — so it lands in `.flash.rodata`, which docs/budget.md:1121 states explicitly: "The art budget has 1,017 B of margin and it is FLASH, not globals". There is no `.bss` array anywhere sized by the roster: every runtime user iterates `SPECIES_TABLE_COUNT` without allocating (Errata/src/game/dex.cpp:65, Errata/src/ui/screen_dex.cpp:52, Errata/src/game/battle.cpp:302, Errata/src/data/encounter_table.h:278). `ui/screen_dex.cpp`'s only file-scope state is `static uint8_t s_cur = 1;` (screen_dex.cpp:42) — one byte, already spent.

THE GOLDEN THAT MOVES. `screen_dex.cpp:103-105` renders the header tag as `"%u/%u"` of `dex_count_caught()` and `SPECIES_TABLE_COUNT`, so `tests/golden/screens/dex_known.pbm` and `dex_unknown.pbm` change from "n/60" to "n/63". That drags `docs/manual/assets/screens/dex_known.svg` / `dex_unknown.svg` (pbm2svg `--check`, tools/check.sh:2675) and `docs/index.html` (build_wiki.py `--check`, tools/check.sh:2713) — i.e. it fires the CLAUDE.md guide rule mechanically.

THE PROSE THAT LIES AND THAT NOTHING CATCHES. `docs/manual/content/guide.typ` says "sesenta" at lines 787, 799, 820, 1090 and 1241; `docs/index.html` carries "sesenta" at 4 places and `tools/wiki/page.html:316` hard-codes `Veinte familias, sesenta bichos` / `Twenty families, sixty bugs` in BOTH languages. `tools/wiki/page.html:162-163` also reasons about "twenty of them" in a CSS comment. `docs/CONTEXT.md:99` says "Hay sesenta especies en veinte familias"; `docs/CONTEXT.md:179` and `docs/creature_style.md` (the "sixty bodies" claims at lines 12, 41, 296-area) say the same. None of this is gated.

THE CONTENT PACK'S OWN GATE IS THE LARGEST SINGLE OBSTACLE. `cd tools/content && python3 verify.py --fast` currently prints `RESULT: 118 checks, 0 FAILED` (measured just now). At least ELEVEN of those checks are hard-pinned to 60/20 and would fail on a 21st family: verify.py:103 `"60 species"`, :104 `"20 families x exactly 3 stages"`, :115 `"every stage-0 spawnable somewhere" == 20`, :125 `"60 distinct names"`, :151-152 `"20 topologically distinct family roots"`, :157 `"types balanced 20/20/20"`, :160-161 `>= 6 constant / <= 6 shift`, :257 `"40 rules = 20 families x 2 steps"`, :268-269 `pat[(10,20)] >= 12`, :280-281 `"rarity distribution 30/18/9/3"`, :286-287 `"5 compat groups x 4 families"`.

AND ONE OF THOSE IS NOT REPARAMETERIZABLE WITHOUT A DESIGN DECISION. verify.py:157 wants 20/20/20 across SIGNAL/CORRUPT/SYSTEM, but verify.py:162-164 requires `v[0].type == v[1].type != v[2].type` for any family that shifts — a family is either constant (3 of one type) or shifts once at stage 2 (2+1). **No single family can contribute +1/+1/+1, so exact type balance is arithmetically impossible at 21 families.** It becomes exact again only at 23 families (3 constant families, one per type) — which is 9 bodies = 1,296 B and overruns `ER_DATA_BYTES_MAX`. This is the fork in the road and it has to be decided deliberately, not discovered.

## La costura
THE CHANGE IS NOT IN THE FIRMWARE. Every C++ file that would change is GENERATED. The seam is the four authored inputs plus two hand-typed literals plus the pinned tests.

AUTHORED INPUTS (the only files a human writes):
1. `/home/user/Pebblebol/tools/content/species.json` — three new rows, ids 61/62/63, family 21, stages 0/1/2, appended in id order (the list is walked in file order; `Content._check_prefix` at tools/gen_content.py:120-129 requires `ids == range(1, n+1)`).
2. `/home/user/Pebblebol/tools/content/evolution.json` — two new rules appended at the END (indices 40 and 41), `{species:61,target:62,...}` and `{species:62,target:63,...}`. Order is contractual: `EVOLUTION_RULES[i]` must be the rule species i points at (tools/gen_content.py:130-135, re-checked at tools/content/verify.py:258-259 and asserted in data/evolution_table.h:190).
3. `/home/user/Pebblebol/tools/sprites/<name>.txt` x3 — three new 24x24x2 bodies.
4. `/home/user/Pebblebol/tools/sprites/atlas.txt` — three lines APPENDED at the end of the species block (the block is the tail in roster order; atlas.txt:38-44 states the contract, `check_species_binding()` at tools/gen_sprites.py:463-506 enforces it).

HAND-EDITED LITERALS (two, and only two):
5. `/home/user/Pebblebol/tools/gen_content.py:73` — `ROSTER_FAMILIES = 20` -> `21`, and the banner at :56-66 rewritten (it currently claims `SPRITE_DATA_BYTES_MAX` must be raised, which is false at 21).
6. `/home/user/Pebblebol/Errata/src/core/nt_types.h:425` — `#define DEX_BYTES 15` -> `16`, with the justification comment at game/dex.h:41-47 updated ("Sixty species is 120 bits is fifteen bytes" -> 63/126/16) and save_schema.h:306-312's "60 species x 2 bits ... is 15 bytes" corrected.

HAND-EDITED MEASURED LITERAL (one, and it is re-measured not invented):
7. `/home/user/Pebblebol/Errata/src/data/sprites.h:395` — `SPRITE_DATA_BYTES_DECLARED 10247u` -> `10679u` (9,648 generated atlas + 1,031 survivors). Its `static_assert(SPRITE_DATA_BYTES == SPRITE_DATA_BYTES_DECLARED, ...)` at sprites.h:398 is what makes forgetting this a named build failure. The banner arithmetic at sprites.h:354-375 must be rewritten with the new margin (585 B = four more sets, not seven). `SPRITE_DATA_BYTES_MAX 11264u` at sprites.h:396 and `ER_DATA_BYTES_MAX = 10240` at tools/gen_sprites.py:95 DO NOT MOVE.

REGENERATED, NEVER HAND-EDITED (`gen_content.py --check` / `gen_sprites.py --check` diff these byte-for-byte in tools/check.sh:162, :212):
- `Errata/src/data/species_table.h` (3 rows, `SPECIES_FAMILY_COUNT`, `SPECIES_PACK_COUNT`, `SPECIES_BASE_OF_FAMILY[21]`, `SPECIES_SPAWN_SUM[6][4]`)
- `Errata/src/data/evolution_table.h` (2 rows, `EVOLUTION_RULES_COUNT`)
- `Errata/src/data/content_version.h` (`CONTENT_VERSION`, a hash of the JSON)
- `Errata/src/data/encounter_table.h`, `creator_schema.h` (re-walk the roster)
- the marked block inside `Errata/src/core/strings_es.h` (6 new StrIds: `STR_SPC_NAME_61..63`, `STR_SPC_FLAV_61..63`)
- `Errata/src/data/sprites_bugs.h` (3 sets: enum ids 64/65/66, `ER_SPRITE_SET_COUNT 67`, `ER_SPRITE_BODY_COUNT 63`, `ER_SPRITE_DATA_BYTES_DECLARED 9648u`, `ER_SPRITE_ART_HASH`, 3 `NT_SPR_SET_FITS` lines, 3 eye-band rows)
- `docs/index.html` (`tools/build_wiki.py`), `docs/manual/assets/screens/dex_*.svg` (`tools/pbm2svg.py --all`), `docs/uso.pdf` (`tools/build_manual.sh`)
- `tests/golden/screens/dex_known.pbm`, `dex_unknown.pbm` (re-recorded)

THE GATE FILE THAT MUST BE REPARAMETERIZED: `/home/user/Pebblebol/tools/content/verify.py` — eleven checks listed in `exists_today`.

THE THREE TEST FILES THAT CARRY PINNED NUMBERS:
- `/home/user/Pebblebol/tests/test_content.cpp:339` `CHECK_EQ((int)SPECIES_TABLE_COUNT, 60);` -> 63 (the surrounding case `the_pack_is_complete_and_the_whole_pack_ships` at :328-346 is otherwise derived and needs no other edit).
- `/home/user/Pebblebol/tests/test_sprite_pipeline.cpp:601-638` — `64u * 144u`, `9216u`, `10247u`, `11264u - 10247u == 1017u`, `CHECK(1017u >= 7u * 144u)`.
- `/home/user/Pebblebol/tests/test_dex.cpp:124-133` — the case is named `the_whole_roster_fits_in_the_fifteen_bytes` and its body is already derived from `SPECIES_TABLE_COUNT`/`DEX_BYTES`; only the NAME is a lie after the change.

PROSE (the guide agent's half, nothing gates it): `docs/manual/content/guide.typ:787,799,820,1090,1241`; `tools/wiki/page.html:162-163,316` (both `data-es` and `data-en` strings); `docs/CONTEXT.md:99,179`; `docs/creature_style.md` ("sixty bodies", "all sixty", "the twenty families"); `docs/content.md:129-165` (the whole §3, which omits the JSON step and misstates the ceiling); `tools/content/README.md:7,62-66`; `tools/gen_content.py:56-66`; `tools/sprites/atlas.txt:27-36` ("P9-C3 appends 60 lines", "registered all 60").

## Pasos (del lector, POSIBLEMENTE MAL - ver correcciones)
1. DESIGN THE FAMILY BEFORE TOUCHING A FILE. Pick a _family_name and a _root topologically distinct from the 20 existing roots (verify.py:151 checks distinctness of the `_root` string set). Pick the type shape: either CONSTANT (3 of one type) or SHIFT-AT-STAGE-2 (t,t,u) - verify.py:162-164 allows nothing else. Pick rarity COMMON at stage 0 (docs/content.md:88: a family's stage 0 may not be RARE or SPECIAL) then UNCOMMON then RARE or SPECIAL. Pick compat_group from the existing 1..5. Pick 4 moves per stage from the existing 34 attacks, each of the species' OWN type or NEUTRAL, with sum(budget_cost) under ATTACK_BUDGET_BY_STAGE plus the rarity bonus and no move over POWER_CAP_BY_STAGE. Base stats must total EXACTLY 16/22/28 with each stat in 1..10.
2. WRITE THE THREE species.json ROWS, ids 61/62/63, appended in id order. Emitted keys: id, family:21, stage:0/1/2, type, base_hp/atk/def/spd, moves[4], evo_rule (40 for stage 0, 41 for stage 1, 255 for stage 2 - the formula is (family-1)*2+stage, checked at tools/gen_content.py:126-129), rarity, spawn_weight (1..255), compat_group, category_mask (must set at least one NET_CATEGORY_BITS bit or the species is unspawnable), sprite_id (60/61/62 - ALWAYS id-1), name (<=9 glyphs, Latin-1, a Spanish proper noun, not in verify.py's DICT_ES list, not a Pokemon term), flavor_es (<=25 glyphs). Design-note keys, not emitted and deliberately outside CONTENT_VERSION: _family_name, _archetype (one of the 6 existing), _rarity_name, _categories, _base_total, _move_names, _move_budget, _budget_cap, and the three the artist draws from - _root (the family's shared topology, one string repeated on all three rows), _silhouette (per-stage, what the 24x24 holds), _frame2 (per-stage, what moves between the two frames). verify.py:148-149 fails if _root/_silhouette/_frame2 are missing.
3. WRITE THE TWO evolution.json RULES, appended at the end of the array: {species:61,target:62,level:10,cond:"NONE",cond_value:0,_from,_to,_family} and {species:62,target:63,level:20,...}. Levels must be one of the three patterns (10,20), (8,18) or (12,24) - verify.py:267-269. Use cond "NONE" unless you intend to spend one of the at-most-5 conditional families (verify.py:270-271) - do not use CORRUPTED, which is pinned to exactly 2 rules (verify.py:272-273, balance.json CORRUPTION.evo_rules, re-checked at verify.py:736-740).
4. DRAW THE THREE BODIES into tools/sprites/<slug>.txt. Filename is the species name folded by slug() (tools/gen_sprites.py:448-460: uppercase, Latin-1 accents to base letters, everything else dropped) and lowercased; the file's `name:` key must equal that slug exactly. Header keys are EXACTLY name/size/frames/species and no others: `name: FOO`, `size: 24x24`, `frames: 2`, `species: 61`. Free-form `#` comment block goes BEFORE the first `--- frame` and should carry the row's _root/_silhouette/_frame2 brief. Then `--- frame 0` and `--- frame 1`, each exactly 24 rows of exactly 24 characters drawn from `#` (ink) and `.` (empty) only - no spaces, no tabs, no trailing whitespace, no blank lines inside a block, UTF-8/LF. Feet on the LAST inked row, empty rows at the TOP. Frame 1 must differ from frame 0 in at least one byte.
5. RUN `python3 tools/gen_sprites.py --self-check tools/sprites/<each>.txt` AND READ THE RENDER. It prints the two frames side by side plus, per frame: ink count and box, N piece(s) (4-connected runs - a number you did not intend is a part that came loose), N hairline px (1px-wide ink, which reads as dirt at 1x), and the derived eyes band with how many pixels the blink closes. At this point the files are not in atlas.txt yet so it prints `NOTE: ... is not in tools/sprites/atlas.txt yet` and still exits 0 - that NOTE is expected here and is the only time it is.
6. APPEND THE THREE NAMES to tools/sprites/atlas.txt at the END of the species block, in id order, under a `# family 21 - <NAME>` comment matching the existing style. The species block is the TAIL and is gapless: entry k of the block must declare `species: k+1` and its set name must equal slug(pack_name_for_that_id). gen_sprites.py:463-506 refuses out-of-order, gapped, unbound-inside-the-block, unknown-id and name-mismatch, each with its own sentence.
7. RAISE DEX_BYTES 15 -> 16 in Errata/src/core/nt_types.h:425 and update the justification prose at Errata/src/game/dex.h:41-47 and Errata/src/persistence/save_schema.h:306-312. Nothing else moves: ConfigV2 stays 256 B, offsetof(ConfigV2,dex)==102 still holds, reserved[] shrinks from 137 to 136, no SAVE_SCHEMA_VERSION bump, old blobs still read zeros.
8. RAISE ROSTER_FAMILIES 20 -> 21 in tools/gen_content.py:73 and rewrite its banner at :22-66 - specifically the sentence at :60-66 that says a 21st family needs SPRITE_DATA_BYTES_MAX raised. It does not, and saying so keeps the next reader from spending a ceiling they do not need to spend.
9. REGENERATE, ONE AT A TIME: `python3 tools/gen_sprites.py` then `python3 tools/gen_content.py`. Read the new ER_SPRITE_DATA_BYTES_DECLARED (expect 9648u) and ER_SPRITE_SET_COUNT (expect 67) out of Errata/src/data/sprites_bugs.h rather than assuming them.
10. UPDATE SPRITE_DATA_BYTES_DECLARED in Errata/src/data/sprites.h:395 to the new measured total (9,648 generated + 1,031 survivors = 10,679) and rewrite the budget banner at sprites.h:349-375: the END STATE line, the margin line (585 B = four more 24x24x2 sets, not seven) and the percentage-of-flash sentence. Do NOT touch SPRITE_DATA_BYTES_MAX (11264) or tools/gen_sprites.py:95 ER_DATA_BYTES_MAX (10240).
11. REPARAMETERIZE tools/content/verify.py's eleven pinned checks so each one still states a PROPERTY rather than a number: :103 len(SPECIES)==3*len(fam); :104 len(fam)==21 derived from max family id; :115 spawnable stage-0 count == len(fam); :125 distinct names == len(SPECIES); :151 distinct _root values == len(fam); :157 the type-balance check (see the decision below); :160-161 constant/shift family counts as a FRACTION of len(fam); :257 len(EVO)==2*len(fam); :268 pat[(10,20)] >= 0.6*len(fam); :280 rarity as proportions of len(SPECIES) with a stated tolerance; :286 5 compat groups each holding 4 or 5 families with the >=2-types spread preserved. THE TYPE-BALANCE CHECK IS A DECISION, NOT A REFACTOR: state it as 'no type is more than one family (3 species) away from an even third' and write the sentence into the check's name, because 20/20/20 cannot hold at 21 families.
12. UPDATE THE THREE PINNED C++ NUMBERS: tests/test_content.cpp:339 60 -> 63; tests/test_sprite_pipeline.cpp:601-638 (64u*144u -> 67u*144u, 9216u -> 9648u, 10247u -> 10679u, the 1017u margin -> 585u, and `CHECK(1017u >= 7u*144u)` -> `CHECK(585u >= 4u*144u)`); rename tests/test_dex.cpp's `the_whole_roster_fits_in_the_fifteen_bytes` to say sixteen.
13. ADD ONE NEW NAMED TEST, because nothing above is specific to the new family. In tests/test_content.cpp: a case that walks EVERY family (not just 21) and asserts each has exactly one stage-0 row whose rarity is COMMON or UNCOMMON, a non-zero category_mask, exactly 2 evolution rules reaching its stage-1 and stage-2 rows, and a sprite_id == id-1 body inside ER_SPRITE_BODY_COUNT. Written over SPECIES_FAMILY_COUNT it protects family 21 and every family added after it.
14. RE-RECORD THE GOLDENS: `make -C tests capture` then re-record tests/golden/screens/dex_known.pbm and dex_unknown.pbm (they carry the '/60' header tag from screen_dex.cpp:104), then `python3 tools/pbm2svg.py --all`, then `python3 tools/build_wiki.py`, then `tools/build_manual.sh` (typst 0.15.1 IS installed here, so --verify will not silently skip the PDF).
15. RUN THE THREE GATES ONE AT A TIME, never two builds at once: `make -C tests check`, then `tools/check.sh`, then `tools/build_matrix.sh`. Record the release flash and globals numbers build_matrix prints and put BOTH in the commit message against 1,363,996 / 61,276.
16. LAST, AND NOT IN PARALLEL WITH THE CODE: Agent(subagent_type: "guide", prompt: "family 21 <NAME> added, roster 60 -> 63 species / 20 -> 21 families; the WIKI header now reads n/63; dex_known.svg and dex_unknown.svg regenerated; both artefacts - the A6 booklet AND docs/index.html - say 'sesenta'/'sixty'/'veinte familias' in prose that is now false: guide.typ 787/799/820/1090/1241, tools/wiki/page.html 162-163 and 316 in both languages, plus the new family needs a bestiary entry and a sentence in the guide, not just a picture"). The guide agent needs the FINAL goldens, which is why it runs last.

## Guardas que dispararán
- COMPILE-TIME, FIRES FIRST: `static_assert((int)SPECIES_TABLE_COUNT * DEX_BITS_PER_SPECIES <= DEX_BYTES * 8, "the roster outgrew the wiki's bit array...")` (Errata/src/game/dex.h:48-51). 63 species x 2 bits = 126 > 120. This is THE guard that stops a 21st family cold and it is why DEX_BYTES 15 -> 16 is step 7.
- COMPILE-TIME: `static_assert(species_sprites_resolve(), ...)` (Errata/src/game/species.cpp:48-50) - three independent failures in one predicate: sprite_id past the atlas, ER_SPRITE_BODY_FIRST + sprite_id past ER_SPRITE_SET_COUNT, and sprite_id != id-1. Fires if ROSTER_FAMILIES is raised without drawing the three bodies.
- COMPILE-TIME: `static_assert(species_bodies_are_creatures(), ...)` (Errata/src/game/species.cpp:75-77) - every row at every stage must resolve into SPRITE_BODY_FIRST..SPRITE_BODY_LAST and never onto an egg or a pose set.
- COMPILE-TIME: `static_assert(SPECIES_TABLE_COUNT == SPECIES_FAMILY_COUNT * 3u, ...)` and `SPECIES_TABLE_COUNT <= SPECIES_ID_BUILTIN_MAX` and `SPECIES_TABLE[N-1].id == SPECIES_TABLE_COUNT` and `species_rows_are_well_formed()` and `species_family_bases_resolve()` and `species_spawn_sums_are_usable()` (Errata/src/data/species_table.h:312-325).
- COMPILE-TIME: `static_assert(EVOLUTION_RULES_COUNT == (uint8_t)(SPECIES_FAMILY_COUNT * 2u), ...)` (Errata/src/data/evolution_table.h:190). Two new rules or the build fails.
- COMPILE-TIME: `builtin_rows_respect_the_creator_budgets()` in Errata/src/data/creator_schema.h:120-145 - every new row's stat total must be EXACTLY CREATOR_STAT_POINTS_BY_STAGE[stage] (16/22/28), each stat 1..10, each move id 1..ATTACK_COUNT, and the learnset inside the stage attack budget. This is where a badly-typed stat lands, not in verify.py (tools/content/README.md:22-26 says verify.py does NOT check stat totals against the table).
- COMPILE-TIME: `static_assert(sizeof(ConfigV2) == 256)` + `static_assert(offsetof(ConfigV2, dex) == 102)` + `static_assert(offsetof(ConfigV2, crc16) == 254)` (Errata/src/persistence/save_schema.h:318-326). Growing dex[] past reserved[] or moving it fails here.
- COMPILE-TIME, PER NEW BODY: `NT_SPR_SET_FITS(bug_spr_<name>, ER_SPRITE_SETS, BUGSPR_<NAME>)` (emitted by tools/gen_sprites.py:967-971) - ties each table row to the array it points at, because drawXBM() reads ((w+7)>>3)*h bytes with no bound of its own.
- COMPILE-TIME: `static_assert(ER_SPRITE_DATA_BYTES == ER_SPRITE_DATA_BYTES_DECLARED)` and `<= ER_SPRITE_DATA_BYTES_MAX` (sprites_bugs.h, emitted at tools/gen_sprites.py:984-987); `static_assert(SPRITE_DATA_BYTES == SPRITE_DATA_BYTES_DECLARED)` and `<= SPRITE_DATA_BYTES_MAX` (Errata/src/data/sprites.h:398-402). The first pair is generated and self-consistent; the SECOND PAIR IS HAND-TYPED and is what makes forgetting sprites.h:395 a named build failure rather than a stale comment.
- COMPILE-TIME: `static_assert(pb_sprite_eyes_fit(), "an eye band runs outside the sprite it indexes")` (sprites_bugs.h tail).
- COMPILE-TIME: `static_assert(NT_ARRAY_LEN(ES) == (size_t)STR_COUNT)` (Errata/src/core/strings_es.h:1657) and `static_assert(STR_MENU_SETTINGS - STR_MENU_BUG + 1 == MENU_ITEM_COUNT)` (:1670) - the contiguous menu block CLAUDE.md warns about is untouched by species strings, but the ES[]/STR_COUNT equality covers the 6 new StrIds.
- GENERATOR, REFUSES TO EMIT: `Content._check_prefix()` (tools/gen_content.py:120-135) - ids not contiguous 1..N; evo_rule != (family-1)*2+stage; EVOLUTION_RULES[i] not the rule species i points at.
- GENERATOR, REFUSES TO EMIT: `_check_field_widths()` (tools/gen_content.py:152-192) - every emitted integer bound-checked against the struct field it lands in, naming the file, the row and the column.
- GENERATOR, REFUSES TO EMIT: `check_species_binding()` (tools/gen_sprites.py:463-506) - five distinct sentences: an unbound set inside the species block, a set whose `species:` is not its position in the block, a species the pack does not hold, a set name that is not slug(pack name), and (via load_sets, tools/gen_sprites.py:409-422) a .txt with no atlas line or an atlas line with no .txt.
- GENERATOR, REFUSES TO EMIT: the art budget check at tools/gen_sprites.py:1133-1138 - over ER_DATA_BYTES_MAX it names the overrun and tells you to raise BOTH ceilings. At 21 families it does not fire (9,648 of 10,240); at 23 families it does.
- GENERATOR, PER-FILE ART CONTRACT: `gen_sprites.py --self-check` hard failures - a row that is not exactly `width` chars, a frame that is not exactly `height` rows, a character that is not `#` or `.`, a tab, trailing whitespace, a blank line mid-frame, a frames: count that disagrees, out-of-order frame numbers, an all-`.` frame, name != filename, an unknown header key, a species body that is not 24x24, a species body without exactly 2 frames, `frame 1 is byte-identical to frame 0`, and the per-file SLOT check.
- GATE (tools/check.sh:160-161): `tools/content/verify.py --fast` - currently 118 checks, 0 FAILED. At least eleven of them fail on a 21st family until reparameterized.
- GATE (tools/check.sh:162-164): `gen_content.py --check` - regenerates 8 headers in memory and byte-diffs the tree. Catches a hand edit to a generated header and a JSON edit that was never regenerated.
- GATE (tools/check.sh:172-174): `gen_content.py --selftest` - proves CONTENT_VERSION actually moves for each input file, including that a change to the roster size moves it (tools/gen_content.py:674-687).
- GATE (tools/check.sh:212-214): `gen_sprites.py --check` - byte-compares Errata/src/data/sprites_bugs.h against the .txt sources in both directions.
- GATE (tools/check.sh:225-227): `gen_sprites.py --self-check` over the WHOLE tree - manifest, species binding, budget, identical frames.
- GATE (tools/check.sh:2662-2677 and 3808-3822): `make -C tests capture` then `pbm2svg.py --check` - the dex goldens change from '/60' to '/63', so docs/manual/assets/screens/dex_known.svg and dex_unknown.svg are stale until regenerated.
- GATE (tools/check.sh:2712-2714): `build_wiki.py --check` - docs/index.html is generated from the roster, the sprites and the screen captures; it goes stale the moment species.json grows.
- GATE (tools/check.sh:3898-3901): `build_manual.sh --draft --verify` - docs/uso.pdf against its sources. typst 0.15.1 IS installed in this container, so unlike the two commits CLAUDE.md warns about, --verify will not silently skip the PDF here.
- GATE (tools/build_matrix.sh:76 and tools/check.sh:30-39): GATE_FLASH_MAX 2,400,000 / GATE_GLOBALS_MAX 90,000 on baseline, GATE_RELEASE_FLASH_MAX 1,600,000 / GATE_RELEASE_GLOBALS_MAX 65,000 on release (Errata/src/core/config.h:689-699). Six variants, zero warnings.
- HOST TEST, WILL FAIL UNTIL UPDATED: tests/test_content.cpp:339 `CHECK_EQ((int)SPECIES_TABLE_COUNT, 60)`; tests/test_content.cpp:344 `CHECK_EQ(SPECIES_TABLE_COUNT, ER_SPRITE_BODY_COUNT)` (this one is DERIVED and is the good guard - it fails by name if you write the JSON rows and forget a body); tests/test_sprite_pipeline.cpp:601-638 (five pinned literals).
- HOST TEST, ALREADY DERIVED AND WILL COVER THE NEW FAMILY FOR FREE: test_content.cpp's every_network_category_has_something_to_spawn, every_family_has_exactly_one_base_stage_and_it_is_reachable, every_shipped_name_fits_the_box_the_screen_gives_it, every_builtin_row_respects_the_budgets_the_creator_is_held_to, every_unreachable_attack_belongs_to_a_family_not_yet_shipped (test_content.cpp:512-537, pinned at 0 - a new family must not orphan an attack and cannot un-orphan one); test_dex.cpp's the_whole_roster_fits_in_the_fifteen_bytes.

## Riesgos
- EXACT TYPE BALANCE CANNOT SURVIVE 21 FAMILIES, AND THIS IS ARITHMETIC, NOT OPINION. verify.py:157 wants SIGNAL/CORRUPT/SYSTEM at 20/20/20; verify.py:162-164 requires any type-shifting family to be (t, t, u) - shift at stage 2 only. So one family contributes 3/0/0 or 2/1/0, never 1/1/1. Balance returns exactly only at 23 families, which is 9 bodies = 1,296 B and blows ER_DATA_BYTES_MAX (9,216 + 1,296 = 10,512 > 10,240). The risk is that whoever does this quietly relaxes the check into vacuity ('at least one of each type') rather than restating the property with a stated tolerance. Mitigation: the reparameterized check must be mutation-tested - make family 21 all-SIGNAL AND flip two existing families to SIGNAL and confirm it fails by name.
- THE SAME SHAPE APPLIES TO rarity 30/18/9/3 (verify.py:280-281) AND 5 compat groups x 4 families (verify.py:286-287). 63 species cannot be 30/18/9/3, and 21 families cannot be 5 x 4. Both must become proportional-with-tolerance, and both are places where a weakened check reads as coverage. Each one needs its own mutation.
- CONTENT_VERSION MOVES, AND IT GATES PEER BATTLES. It is a hash of the JSON (tools/gen_content.py:2459-2464) and battle setup refuses a mismatch outright: `setup.content_ver != (uint16_t)CONTENT_VERSION` returns BR_VERSION_MISMATCH (Errata/src/game/battle.cpp:347 and :1032), and it is mixed into the battle hash at battle.cpp:95. A 21-family device and a 20-family device cannot battle or link. Nothing in the tree is wrong about this - it is the designed behaviour - but it is a real product consequence of a content-only change and belongs in the commit message and in the guide agent's brief.
- THE ART IS THE WORK, AND NOTHING IN THE PIPELINE CAN JUDGE IT. tools/sprites/README.md:303-306 says it outright: 'A 24x24 of noise passes every check in this directory.' Three bodies that pass --self-check, --check, every static_assert and the whole gate can still read as three icons rather than three stages of one creature. The instruments that can tell you are the --self-check render (piece count, hairline count, blank rows under the body, eye band and how many pixels the blink closes) and `make -C tests spritetool && ./tests/bin/sprite_dump text NAME` / `sprite_dump blink NAME` / `sprite_dump sheet`, which decode the COMPILED header the way the device's drawXBM does. Budget real time for this; P9-C3 redrew PIXIO entirely at integration for exactly this reason (tools/sprites/pixio.txt:3-8).
- THE BLINK IS DERIVED FROM THE PIXELS AND CANNOT BE OVERRIDDEN. gen_sprites.py finds 'the biggest group of enclosed holes that can be closed together WITHOUT CLOSING ANYTHING THAT IS NOT A HOLE' (tools/sprites/README.md:247-260). Any small enclosed hole - a porthole, a bolt, a bite - will be read as an eye. If the blink lands wrong the fix is to MOVE THE HOLE, not add a key; there is no key to add. A body with no small enclosed hole gets {255,0,0,0} and does not blink at all, which is legal but is a design outcome you should choose rather than discover.
- THE 'sesenta' PROSE IS THE FAILURE MODE CLAUDE.md NAMES BY EXAMPLE. Five lines of guide.typ, four of docs/index.html, and tools/wiki/page.html:316 in BOTH Spanish and English hard-code the count. None of it moves a pixel or a string id, so pbm2svg --check, build_wiki.py --check and build_manual.sh --verify all pass with the booklet and the website saying sixty. This is the LUZ defect exactly. It is the guide agent's job and it must be given the specific line numbers, not a general instruction.
- docs/content.md:151-165 AND tools/gen_content.py:56-66 ARE BOTH WRONG ABOUT THE CEILING AND SILENT ABOUT THE JSON. They say a 21st family needs SPRITE_DATA_BYTES_MAX and ER_DATA_BYTES_MAX raised (it does not, at 21 or 22) and they do not mention writing species.json/evolution.json rows at all, because they were written when the pack was ahead of the ship. Following them literally produces a commit that raises two ceilings it does not need and emits an empty prefix. Both must be corrected in this commit or the next person repeats it.
- THE CHECK.SH SPRITE GATE IS SKIPPABLE. tools/check.sh:229-230: if python3 is missing it prints 'SKIPPING the sprite gate' to stderr and the gate still says GATE OK. python3 is present here, but the pattern is the same one CLAUDE.md flags for typst - a skip that reads as a pass. Confirm the gate ran, do not just read the last line.
- THE `used[64]` ARRAY IN tests/test_content.cpp:514 IS SIZED BY ATTACK COUNT (34), NOT BY SPECIES, so it does not overflow at 63 species. I checked. But the neighbouring case pins `unreachable == 0` - a new family whose kits happen to be drawn only from already-used attacks keeps it at 0 (fine), while adding a NEW attack for the family without putting it on a learnset breaks it by name. That is an argument for cutting new attacks from this commit.
- THE SPAWN POOLS ARE RE-DERIVED, AND A NEW FAMILY DILUTES EVERY EXISTING SPECIES' ENCOUNTER PROBABILITY. SPECIES_SPAWN_SUM[6][4] is regenerated (species_table.h:246-250), so every species in the new family's categories/rarity bands gets a slightly lower share. verify.py's FULL (non---fast) run checks 'no species is unreachable in practice (common<=60d, unc<=120d, rare<=150d, special<=60d)' with worst-case days (verify.py:735-742) and 'every species is reachable by a wild encounter' (verify.py:728). tools/check.sh only runs --fast, so THE FULL RUN IS NOT IN THE GATE and must be run by hand once for this commit. A high spawn_weight on the new family can push an existing SPECIAL past its 60-day chase limit without anything in the gate noticing.

## Incógnitas bloqueantes
- THE TYPE-BALANCE DECISION IS A PRODUCT CALL AND I SHOULD NOT MAKE IT ALONE. Either (a) accept an uneven roster at 21 families and restate verify.py:157 as a tolerance ('no type more than one family away from an even third'), or (b) do three families at once for 23/23/23 and pay for raising BOTH ER_DATA_BYTES_MAX and SPRITE_DATA_BYTES_MAX. (a) is one commit; (b) is not. This needs an answer before any JSON is written, because it decides the new family's three types.
- WHAT THE 21st FAMILY IS. The roster's twenty families are PACKET, RADIO, LATENCY, PIXEL, SPAM, BUG, WORM, PHISH, NULL, BITROT, DAEMON, FIREWALL, MEMORY, FILE, PING, GLITCH, PORT, CRYPTO, PROBE, COOKIE (tools/sprites/atlas.txt:46-139). A 21st needs a computer-defect concept, three Spanish proper-noun names of <=9 glyphs each that are not dictionary words and not Pokemon terms, three <=25-glyph flavour lines, and a `_root` topology genuinely distinct from the twenty already used (verify.py:151-152 compares the `_root` STRINGS, so a paraphrase of an existing root passes the check and still looks like a copy on the contact sheet).
- WHETHER THE RELEASE BUILD STILL MEASURES 1,363,996 / 61,276. That figure is CHANGELOG.md:308-309 at the current HEAD of `claude/repo-exploration-sync-bbonku`; docs/budget.md:1203 is two commits behind it at 59,468. arduino-cli and esp32:esp32 3.1.1 are installed, so `tools/build_matrix.sh` can confirm it, but I did not run it (a six-variant matrix is a long build and the task was to map). The commit must re-measure rather than carry my 760 B estimate forward.
- WHETHER ANY OF THE 85 CAPTURED SCREENS OTHER THAN dex_known/dex_unknown MOVES. I traced screen_dex.cpp:104's '%u/%u' as the only place SPECIES_TABLE_COUNT reaches a drawn string, but the only way to know is `make -C tests capture` and diff - and the capture is build output that is not committed, so it cannot be checked by reading. Do this before assuming two SVGs.
- WHETHER THE FULL verify.py RUN (no --fast) IS GREEN TODAY. tools/check.sh only runs --fast, so the simulator sections (11-14: the AI-vs-AI matrix, the per-species reachability days, the corruption reachability) are not under any gate and I did not run them - they are slow. If they are already failing or marginal at 60 species, a 21st family's spawn weights will be blamed for something that predates it. Establish the baseline BEFORE adding the family.
- WHETHER `SPRITE_DATA_BYTES_DECLARED` 10,679 is exactly right. It is 9,648 (67 sets x 144) + 1,031 (survivors: spr_icon12 384 + spr_mini8 168 + spr_badge12 312 + emotes 167, each term asserted at tests/test_sprite_pipeline.cpp:617-625). That arithmetic is sound but the literal must come from the compiler's own static_assert failure message after regeneration, not from this arithmetic - sprites.h:339-347 records that this exact number was wrong by 144 B for two phases because a human typed it.

## Alcance recomendado
ONE FAMILY. ONE COMMIT. ZERO NEW GLOBALS, ZERO CEILINGS RAISED.

WHAT A GOOD VERSION LOOKS LIKE

A 21st family: three species (ids 61/62/63), three 24x24x2 bodies, two evolution rules, `ROSTER_FAMILIES` 20 -> 21, `DEX_BYTES` 15 -> 16, `SPRITE_DATA_BYTES_DECLARED` re-measured to 10,679, `tools/content/verify.py`'s eleven pinned checks restated as properties, three pinned C++ literals updated, one new derived test, two goldens re-recorded, both player artefacts regenerated, and the guide agent run last. Flash cost ~760 B of 236,004 free (0.3 %). Globals cost 0 of 3,724 free, and that zero is a claim the commit must prove with `build_matrix.sh` output, not assert.

The family must be CONSTANT-TYPE or SHIFT-AT-STAGE-2, stage 0 COMMON with a non-zero `category_mask`, `compat_group` from the existing 1..5, four moves per stage drawn only from the existing 34 attacks, base totals exactly 16/22/28. It adds no attack, no item, no evolution condition, no encounter row, no network category, no screen and no buffer.

THE TESTS THAT MUST FAIL WHEN THIS IS BROKEN - each paired with the specific mutation that must make it fail BY NAME, because a test that survives the break protected nothing:

1. Delete one of the three atlas.txt lines -> `tests/test_content.cpp::the_pack_is_complete_and_the_whole_pack_ships` fails on `CHECK_EQ(SPECIES_TABLE_COUNT, ER_SPRITE_BODY_COUNT)` (63 vs 62), and `species.cpp`'s `species_sprites_resolve()` static_assert fires first at build time. Two independent guards, both named.
2. Revert `DEX_BYTES` to 15 -> `game/dex.h:48`'s static_assert fires with its own sentence ("the roster outgrew the wiki's bit array"), and `tests/test_dex.cpp::the_whole_roster_fits_in_the_sixteen_bytes` cannot compile. The runtime half must still count 63 seen and 63 caught.
3. Copy an existing body file under the new name -> `gen_sprites.py --self-check` must print "says species: N, which is <X> in the pack, so this set must be named <X>", and `tests/test_sprite_pipeline.cpp::no_two_species_bodies_hold_the_same_pixels` must fail. If --self-check says OK, the SLOT check is not covering the new ids.
4. Leave `SPRITE_DATA_BYTES_DECLARED` at 10247 -> `sprites.h:398`'s static_assert fails by name. Leave the test's `9216u` -> `test_sprite_pipeline.cpp::the_atlas_is_at_its_end_state_and_the_transition_allowance_is_gone` fails. Both directions.
5. Make family 21's stage 0 RARE -> `test_content.cpp::every_family_has_exactly_one_base_stage_and_it_is_reachable` must fail. Set its `category_mask` to 0 -> `every_network_category_has_something_to_spawn` or the new test must fail.
6. THE NEW TEST (the one thing genuinely added): a case in `tests/test_content.cpp` that walks ALL `SPECIES_FAMILY_COUNT` families and asserts, per family, exactly one stage-0 row of rarity COMMON or UNCOMMON with a non-zero mask, exactly two evolution rules reaching its stage-1 and stage-2 rows, and three bodies at `sprite_id == id - 1` inside `ER_SPRITE_BODY_COUNT`. Written over the constant, it protects family 21 and every family after it. Mutation: break family 7's stage-1 evolution target and it must fail naming family 7.
7. EACH REPARAMETERIZED verify.py CHECK GETS ITS OWN MUTATION. Restating `"types balanced 20/20/20"` as a tolerance is the dangerous one: flip two EXISTING families to one type and the new check must still fail. If it does not, the check has been relaxed into decoration and is worse than deleting it, because it reads as coverage.
8. Leave `guide.typ` saying "sesenta" -> nothing fails, and that is the point. This is the half no gate catches; it is why the guide agent is a step and not a courtesy.

WHAT I WOULD CUT, EXPLICITLY

- THE JUMP TO 66 FAMILIES / 198 SPECIES. That is 45 more families, 135 bodies, ~19,440 B of art against a 1,024 B margin, and a `DEX_BYTES` of 50. It is a phase, not a commit.
- RAISING `ER_DATA_BYTES_MAX` (10,240) OR `SPRITE_DATA_BYTES_MAX` (11,264). Measured: one family lands at 9,648 and 10,679, both inside. Two families still fit. Three do not. Raise them when they bind, and say so then - raising a ceiling you are not touching is exactly the "transition allowance with room to hide in" that `sprites.h:349-353` records as the defect.
- ANY NEW ATTACK, ITEM, EVOLUTION CONDITION, ENCOUNTER ROW OR NETWORK CATEGORY. The 34 attacks are all on a learnset and `test_content.cpp:512-537` pins `unreachable == 0`; a new attack breaks it by name unless it lands on the new kits, which is a second design problem inside the first.
- ANY NEW SCREEN OR BUFFER. The wiki screen already exists (`SCR_WIKI`, one 165-byte module array shared with the manual screen, CHANGELOG.md:40-42) and handles 63 species with no change. Adding a "family view" screen would fire the five-guard `ScreenId` cascade CLAUDE.md warns about and spend from the 3,724 B.
- PER-FAMILY SLEEP/SICK ART. docs/budget.md:1247-1251 already prices it: twenty family sick bodies are 2,880 B and overrun both caps. P10-C3 derived the sleeping pose from each species' own idle body for 0 B; the new family inherits that for free.
- RETUNING THE EXISTING 60 ROWS to restore exact type/rarity/compat balance. That touches 60 rows to make a Python check happy, moves `CONTENT_VERSION` for reasons unrelated to the feature, and invalidates the balance work behind every existing kit. Restate the checks instead, and mutation-test each restatement.
- THE FULL `verify.py` SIMULATION AS A NEW GATE. Run it once by hand for this commit and record the numbers; wiring the slow simulator into `check.sh` is its own change.

THE FIVE LAWS, VERBATIM, from docs/creature_style.md §2 "The five laws" (line 40), which opens: "Every one of the sixty bodies obeys all five. Breaking one is not forbidden, but it is a decision to argue for in the file's header, the way the sixteen `DEVIATIONS` blocks already do."

  2.1 The body is one solid mass
  2.2 The eye is a punched socket
  2.3 Nothing is one pixel
  2.4 The feet are on the last inked row
  2.5 Frame 2 is motion, not flicker

And the sentence they are all consequences of, §1 (line 26): "A solid shape with holes punched in it, that fails a little more at each stage."

The one-line substance of each, in the guide's own words: 2.1 "Ink is filled, not outlined... Interior structure is therefore always subtractive"; 2.2 "An eye is an enclosed unlit region - background the outside cannot reach - of at least 3 px, at most 8 rows tall and at most half the body wide", and "What is never legal is an eye drawn as a lit dot on a solid body, with no socket punched around it"; 2.3 "Two pixels is the floor for any feature that has to read: a bite, a dot, a gap, a limb"; 2.4 "Bodies stand on HOME_FLOOR_Y. Keep the empty rows at the top of the grid, never at the bottom"; 2.5 "a blink is a squint, never a shut eye... because the two frames alternate at even duty, so a fully closed eye reads as flicker."

## Coste: globals 0 B, flash 760 B
MEASURED BY INSPECTION OF STORAGE CLASS AND STRUCT SIZE, NOT BY GUESS, AND THE ANSWER IS EXACTLY ZERO.

Three places a roster change could plausibly spend globals, and none of them does:

1. THE TABLES ARE `.rodata`, NOT `.bss`/`.data`. `SPECIES_TABLE`, `SPECIES_BASE_OF_FAMILY`, `SPECIES_SPAWN_SUM` (Errata/src/data/species_table.h:210-250), `EVOLUTION_RULES` (evolution_table.h:54), `ER_SPRITE_SETS` / `ER_SPRITE_EYES` / `ER_SPRITE_NAMES` / every `bug_spr_*` array (sprites_bugs.h:124, :2172, :2262) and the `ES[]` string table are all `inline constexpr`. docs/budget.md:1121 states the consequence in the project's own words: "The art budget has 1,017 B of margin and it is FLASH, not globals". The same file's §16.1 prices "27 more species with art" purely against the 249 KB flash line, never against globals.

2. THERE IS NO RUNTIME ARRAY SIZED BY THE ROSTER. Every consumer iterates without allocating: game/dex.cpp:65, ui/screen_dex.cpp:52-63, game/battle.cpp:302, game/battle.h:708, game/validate.cpp:72, data/encounter_table.h:278, dev/diag_core.cpp:499, ui/screen_battle.cpp:309. `ui/screen_dex.cpp`'s entire file-scope mutable state is `static uint8_t s_cur = 1;` (screen_dex.cpp:42), which is one byte and already spent. Contrast this with the known globals hogs docs/budget.md:1247-1250 names as droppable (`perf.cpp`'s `uint16_t frame_max[SCR_COUNT]` at 58 B, `diag_core.cpp`'s `kScreenName` at 116 B) - nothing of that shape exists for species.

3. DEX_BYTES 15 -> 16 IS A ZERO-SUM MOVE INSIDE A FIXED STRUCT. `uint8_t dex[DEX_BYTES]; uint8_t reserved[152 - DEX_BYTES];` (save_schema.h:313-314) sits inside a struct held at `static_assert(sizeof(ConfigV2) == 256, "ConfigV2 layout drifted")` (save_schema.h:318). The dex grows by one byte and `reserved` shrinks by one byte; the struct, and therefore whatever instance of it lives in `.bss`, is byte-identical in size before and after. `tests/test_dex.cpp:18` and `tests/test_screens.cpp:416` allocate `uint8_t[DEX_BYTES]` but those are HOST test binaries, not the firmware image.

WHAT I DID NOT DO, STATED PLAINLY: I did not run `tools/build_matrix.sh` to re-measure `.bss + .data` on the release variant. `arduino-cli` and the `esp32:esp32 3.1.1` core ARE installed in this container, so it is runnable, but a six-variant matrix is a long build and the task was to map, not to build. The 3,724 B figure in the brief is confirmed from the project's own record rather than recalled: CHANGELOG.md:308-309 gives the current release line as "1,363,996 / 1,600,000 and 61,276 / 65,000", i.e. 236,004 B of flash free and 65,000 - 61,276 = 3,724 B of globals free. (docs/budget.md:1203-1205's table is two commits stale at 59,468/5,532 and should not be quoted.) THE COMMIT MUST STILL RE-MEASURE AND QUOTE BOTH NUMBERS: a zero-globals prediction that is never checked against the linker is the exact defect this repository's house rule is about.


# EL ESCÉPTICO

## Afirmaciones REFUTADAS
- FALSE: 'verify.py:268-269 pat[(10,20)] >= 12' is listed among the eleven checks that 'would fail on a 21st family'. It cannot fail. Measured from the JSON: pat = {(10,20): 14, (8,18): 3, (12,24): 3}. The check is `set(pat) <= {(10,20),(8,18),(12,24)} and pat[(10,20)] >= 12` (tools/content/verify.py:267-269). Adding one family with any of the three legal level patterns leaves pat[(10,20)] at 14 or 15 and adds no new key. Both halves stay green.
- HALF-FALSE AND BACKWARDS: 'verify.py:160-161 >= 6 constant / <= 6 shift' is listed as failing. Measured: const = 14, shift = 6. `check("at least 6 families with a constant type", len(const) >= 6)` (verify.py:160) CANNOT fail by adding a family. But `check("at most 6 families shift type", len(shift) <= 6)` (verify.py:161) is SATURATED AT EXACTLY 6 TODAY, which the plan never says. That directly contradicts mechanical_steps[0]: 'Pick the type shape: either CONSTANT (3 of one type) or SHIFT-AT-STAGE-2 (t,t,u) - verify.py:162-164 allows nothing else.' Choosing SHIFT makes it 7 and fails verify.py:161 on the spot. Real count of pinned checks that fail at 21 families is NINE (103, 104, 115, 125, 151, 157, 257, 280, 285), not eleven.
- FALSE ARITHMETIC: 'No single family can contribute +1/+1/+1, so exact type balance is arithmetically impossible at 21 families. It becomes exact again only at 23 families - which is 9 bodies = 1,296 B and overruns ER_DATA_BYTES_MAX.' The first sentence is right; the conclusion is wrong. TWO shift families restore exact balance at 22 families: a (t,t,u) family contributes (2,1,0) in some permutation, so family A = (2,1,0) and family B = (0,1,2) sum to (2,2,2), giving 22/22/22. Six bodies is 864 B: 9,216 + 864 = 10,080 <= ER_DATA_BYTES_MAX 10,240 (tools/gen_sprites.py:95) and 10,080 + 1,031 = 11,111 <= SPRITE_DATA_BYTES_MAX 11,264 (Errata/src/data/sprites.h:396). It FITS. blocking_unknowns[0] therefore presents a false dilemma - (a) relax the check at 21, or (b) three families that blow the budget - and hides the option that actually preserves the property inside both ceilings. (Two families still needs DEX_BYTES 17 and still breaks rarity 30/18/9/3 and compat 5x4; that is an argument, not the arithmetic the plan gave.)
- FALSE, AND DISPROVABLE BY COMPUTATION: 'I traced screen_dex.cpp:104's %u/%u as the only place SPECIES_TABLE_COUNT reaches a drawn string' (blocking_unknowns[3]), and the whole 'THE GOLDEN THAT MOVES' section, which names two goldens and two SVGs. Errata/src/ui/screen_battle.cpp:308-309, inside build_foe(): `const uint32_t h = bt_mix(seed + 0x9E3779B9u * (i+1)); const uint8_t sp = (uint8_t)(1u + (h % (uint32_t)SPECIES_TABLE_COUNT));` - called from screen_battle.cpp:792 on every PRACTICE entry. tests/test_screens.cpp drives the battle snapshots with fixed seeds (battle_arm(BT_ENTRY_PRACTICE, 0xB0A71E01u..0xB0A71E07u)). Running the real bt_mix over those seeds: 0xB0A71E01 [51,44,6]->[6,17,48]; 0xB0A71E02 [19,6,7]->[4,39,28]; 0xB0A71E03 [58,5,41]->[16,50,44]; 0xB0A71E04 [60,48,18]->[60,57,45]; 0xB0A71E05 [7,1,58]->[46,37,25]; 0xB0A71E06 [38,37,27]->[38,58,15]; 0xB0A71E07 [7,13,16]->[16,55,19]. EVERY seed changes. The foe's name and its 24x24 body are drawn, so battle_intro/menu/hit/faint/protect/result/pick all move, dragging docs/manual/assets/screens/battle_*.svg (7 files, confirmed present) and docs/index.html.
- FALSE BY OMISSION, AND THIS IS THE BIGGEST ONE: risks[2] says of CONTENT_VERSION 'Nothing in the tree is wrong about this - it is the designed behaviour'. tests/golden/battle_v1.txt line 1 is `seed=2468ACE0 engine=3 content=A78B level=14`, written by tests/test_battle_golden.cpp:123-125 from CONTENT_VERSION, and compared BYTE FOR BYTE by TEST(the_scripted_battle_matches_the_recorded_transcript_byte_for_byte). Worse: Errata/src/game/battle.cpp:91-95, `battle_state_hash()` folds `(uint32_t)CONTENT_VERSION` into battle_hash_basis(), so every `h=` line in the 222-line transcript changes too. The plan never names this file, never names `make -C tests golden`, and the file's own header (tests/test_battle_golden.cpp:16-18) says 'Re-record ONLY from a `retune:` commit - or one that deliberately changes what a round does'. A content-only commit is neither, so this needs a written argument, not a silent re-record.
- OVERSTATED: 'the gen_content.py:62 banner and docs/content.md:160-165 are WRONG that one more family needs SPRITE_DATA_BYTES_MAX raised.' The arithmetic is right, but the tree is not uniformly wrong. Errata/src/data/sprites.h:376-378 already states 'margin 1,017 B SEVEN more 24x24x2 sets (1,008 B): one more three-stage family and four effect sets', and docs/budget.md:1121-1122 repeats it verbatim. Only tools/gen_content.py:60-64 says otherwise. Presenting this as a tree-wide error inflates the correction work and misrepresents two files that are already correct.
- IMPRECISE: 'do not use CORRUPTED, which is pinned to exactly 2 rules (verify.py:272-273...)'. verify.py:272-273 reads `check("CORRUPTED unlocks an evolution in 1-2 families (P9-C5)", 1 <= len(corr) <= 2, ...)` - an inequality at its upper bound, not a pin. The hard `== 2` is verify.py:739, against balance.json's CORRUPTION.evo_rules, a different quantity. The advice survives; the citation does not.
- INCOMPLETE: the prose list names guide.typ:787, 799, 820, 1090, 1241 (Spanish only). CLAUDE.md says the booklet is bilingual paragraph by paragraph, and grep confirms the English twins say the same false thing: guide.typ:801 'lists all *sixty* species', :813 'out of sixty', :824 'all sixty show from the start', :1101 'the sixty species and every screen', :1261 'the list of the sixty species'. Ten lines, not five. The guide agent's brief in mechanical_steps[15] would leave half the booklet lying.

## Guardas que el lector NO vio
- tests/test_battle_golden.cpp::the_scripted_battle_matches_the_recorded_transcript_byte_for_byte - fails on line 1 (`content=A78B`) the moment species.json changes, and then on ~40 more `h=` lines because battle.cpp:95 mixes CONTENT_VERSION into battle_state_hash(). Re-recorded only by `make -C tests golden` (tests/Makefile:1023-1025), which the plan never mentions.
- The seven practice-battle screen goldens - tests/golden/screens/battle_{pick,intro,menu,hit,faint,protect,result}.pbm - move because build_foe() (screen_battle.cpp:309) is `1 + (h % SPECIES_TABLE_COUNT)`. Computed above: all seven seeds resolve to different foe teams at 63. Their docs/manual/assets/screens/battle_*.svg twins then fail `pbm2svg.py --check` (tools/check.sh:2675) and docs/index.html fails `build_wiki.py --check` (tools/check.sh:2713).
- The snapshot tests themselves, not just their pixels: tests/test_screens.cpp:3778-3790 asserts CHECK_EQ(battle_screen_mode(), BTM_PICK/BTM_INTRO/BTM_MENU) and battle_to_result() (test_screens.cpp:3770-3772) spins a 4000-iteration guard to reach BTM_RESULT. A different foe team is a different battle; those assertions and that guard are now running an unrehearsed scenario.
- tools/content/verify.py:161 `at most 6 families shift type` is at its ceiling (shift == 6 exactly, measured). The plan offers SHIFT-AT-STAGE-2 as a free design choice in mechanical_steps[0]; it is not - it fails this check immediately.
- tools/content/verify.py:270-271 `evo_cond used by <= 5 families` is also at its ceiling (5 today: DAEMON, COOKIE, GLITCH, CRYPTO, PIXEL). The plan says 'the at-most-5 conditional families' without saying all five are spent.
- `make -C tests capture` (tests/Makefile:796-800) re-records NOTHING committed - it writes tests/capture/golden/screens/, which tools/check.sh:2663-2674 explicitly calls build output that is not committed. The committed goldens are re-recorded by `make -C tests screens` (tests/Makefile:1029-1030). mechanical_steps[13] says 'make -C tests capture then re-record ... dex_known.pbm and dex_unknown.pbm', which names the wrong target and leaves 'how' undefined.
- tools/content/verify.py:252 prints 'distinct 4-move sets: %d/60' and verify.py:530/535 compute `atlas = 60*2*72` and print 'sprite atlas 60 x 2 x 24x24'. Not check() calls, so no FAILED, but they are stale output in the one file whose job is to be read, and the plan's eleven-check reparameterization list does not include them.
- Errata/src/data/evolution_table.h:187 `static_assert(EVOLUTION_RULES_COUNT < SPECIES_EVO_NONE)` - benign at 42 rules, but it caps the rule table at 127 families and nobody has priced it against the '66 families' headline in exists_today.
- tests/test_content.cpp:329 `CHECK(SPECIES_PACK_COUNT >= 60)` - a one-sided bound. Once the pack grows past 60 nothing pins the pack half any more; the plan updates only the ship-half pin at :339.

## Veredicto de coste
GLOBALS: 0 B is CORRECT and I derived it independently rather than taking the plan's word. Every roster-sized table is `inline constexpr` and therefore .rodata/flash on this target: SPECIES_TABLE (species_table.h:85), EVOLUTION_RULES (evolution_table.h:53), ER_SPRITE_SETS / ER_SPRITE_EYES / ER_SPRITE_NAMES / bug_spr_* (sprites_bugs.h:2172, :2262, :124), and ES[] (strings_es.h:899, `inline constexpr const char* const ES[]` - the pointer array is const too, so it does not land in .data). I grepped the whole firmware for a non-const static sized by any roster constant; the only hits are species_custom.cpp:16,22, both sized by CREATOR_SPECIES_SLOTS (fixed 10). DEX_BYTES 15->16 is byte-neutral: `uint8_t dex[DEX_BYTES]; uint8_t reserved[152 - DEX_BYTES];` (save_schema.h:313-314) inside a struct pinned by `static_assert(sizeof(ConfigV2) == 256)` (save_schema.h:318), so dex[16] + reserved[136] is the same 256 B. FLASH: my own build-up, not the plan's - 3 x sizeof(SpeciesDef)=24 -> 72; 2 x sizeof(EvolutionRule)=6 -> 12 (the plan's citations elsewhere use 8 B/rule from verify.py's estimator; the struct is 6, asserted at evolution_table.h:51); 3 x 144 B of bitmap = 432; 3 x sizeof(SpriteSet) (ptr + 3 u8, 8 B aligned) = 24; 3 x 2 x SpriteEyeBand ~ 24; 3 ER_SPRITE_NAMES pointers + literals ~ 36; 6 new strings ~ 108 B of text + 24 B of ES[] pointers + 12 B of index; +1 B in SPECIES_BASE_OF_FAMILY. Total ~745 B before padding. 760 B is defensible. What the number OMITS and should say: nothing in the 760 covers the re-record work, and the commit's real risk is not bytes.

## Veredicto de alcance
NOT one commit as written - it is one commit of content plus a re-record pass the plan does not scope, and one product decision it explicitly defers. Three things break it: (1) tests/golden/battle_v1.txt must be fully re-recorded because CONTENT_VERSION is in its header AND folded into every round hash - and that file's own header forbids re-recording outside a `retune:` commit, so this needs an argument written down, not a `make -C tests golden`. (2) Seven practice-battle screen goldens change (proven by computing bt_mix over the seven fixed seeds), which drags seven SVGs into the printed A6 booklet and docs/index.html - the plan budgeted two of each. (3) blocking_unknowns[0] says the type-balance call 'needs an answer before any JSON is written' and the plan does not answer it; its stated dilemma is also arithmetically wrong (22 families restores exact balance inside both art ceilings). The content half - three JSON rows, three bodies, two rules, DEX_BYTES, ROSTER_FAMILIES, SPRITE_DATA_BYTES_DECLARED, nine reparameterized verify.py checks, three pinned C++ literals - IS one commit and is well-mapped. The golden half is a second, separable commit that must land in the same push because it is what makes `tools/check.sh` green. Recommend splitting the work but landing both, and deciding the type-balance question before touching species.json.

## PASOS CORREGIDOS (usa ESTOS)
1. STEP 0 (NEW, BLOCKS EVERYTHING): answer the type-balance question with the corrected arithmetic. The options are (a) ONE family, accept 21/21/21 is impossible, restate verify.py:157 as a tolerance; (b) TWO families as two shift-at-stage-2 families whose shifts are permuted so the contributions are (2,1,0) and (0,1,2), restoring exact 22/22/22 - 6 bodies = 864 B, landing at 9,216+864 = 10,080 <= 10,240 and 10,080+1,031 = 11,111 <= 11,264, so NEITHER ceiling moves, and DEX_BYTES goes to 17 not 16. The plan's claim that this is only possible at 23 families and only by blowing ER_DATA_BYTES_MAX is wrong. Option (b) also fails verify.py:161 (shift would go 6 -> 8) and still breaks rarity 30/18/9/3 and compat 5x4, so it is not free - but it is the option that preserves the property instead of relaxing the check, and it has to be on the table.
2. STEP 1 (CORRECTED): if you take one family, it MUST be CONSTANT-TYPE. verify.py:161 `at most 6 families shift type` is saturated at exactly 6 today (measured). A shift family is illegal before you write a line of JSON. Also: do NOT give it an evolution condition - verify.py:270-271 `evo_cond used by <= 5 families` is saturated at exactly 5 (DAEMON, COOKIE, GLITCH, CRYPTO, PIXEL).
3. STEPS 2-6 (species.json rows, evolution.json rules, three .txt bodies, --self-check, atlas.txt): keep as written. I checked every contract they cite - gen_content.py:120-135 _check_prefix, gen_sprites.py:463-506 check_species_binding, the evo_rule == (family-1)*2+stage formula, the id-order append, sprite_id == id-1 - and all of them say what the plan says they say.
4. STEPS 7-10 (DEX_BYTES, ROSTER_FAMILIES, regenerate, SPRITE_DATA_BYTES_DECLARED): keep as written; all verified. One correction to step 10's prose: sprites.h:376-378 and docs/budget.md:1121-1122 are ALREADY correct that the 1,017 B margin covers one three-stage family. Only tools/gen_content.py:60-64 needs correcting, not 'the tree'.
5. STEP 11 (CORRECTED COUNT): reparameterize NINE checks, not eleven - verify.py:103, :104, :115, :125, :151, :157, :257, :280, :285. Leave :160 alone (len(const) >= 6 with const == 14 cannot fail) and leave :267-269 alone (pat[(10,20)] == 14 >= 12 and no new level key). Do NOT touch :161 or :270-271 either - instead OBEY them by choosing a constant-type, unconditional family. While in the file, fix the stale prints at :252 ('/60') and :530/:535 (`atlas = 60*2*72`).
6. STEP 12 (pinned C++ literals): as written and verified - tests/test_content.cpp:339 60->63, tests/test_sprite_pipeline.cpp:601-638 (64u*144u->67u*144u, 9216u->9648u, 10247u->10679u, 1017u->585u, CHECK(1017u >= 7u*144u) -> CHECK(585u >= 4u*144u); 585 >= 576 holds), rename tests/test_dex.cpp:124.
7. STEP 13 (the new derived test): keep. Add one clause the plan did not: the new case should also assert `SPECIES_PACK_COUNT == SPECIES_TABLE_COUNT` stays an equality, because test_content.cpp:329's `CHECK(SPECIES_PACK_COUNT >= 60)` is one-sided and stops protecting the pack half the moment the pack grows.
8. STEP 14 (REPLACES the plan's single re-record step, and it is three separate recordings): (a) `make -C tests screens` - re-records tests/golden/screens/*.pbm. Expect dex_known and dex_unknown (header tag n/60 -> n/63) AND battle_pick, battle_intro, battle_menu, battle_hit, battle_faint, battle_protect, battle_result, because build_foe() at screen_battle.cpp:309 folds the foe out of `h % SPECIES_TABLE_COUNT` and all seven fixed seeds resolve differently at 63. READ THE DIFF - a battle golden whose foe changed is expected; one whose LAYOUT changed is a bug. `make -C tests capture` is NOT this step: it writes tests/capture/, which is uncommitted build output (tools/check.sh:2663-2674). (b) `make -C tests golden` - re-records tests/golden/battle_v1.txt, whose header line carries `content=A78B` and whose every round hash folds CONTENT_VERSION via battle.cpp:95. That file's header says re-record only from a `retune:` commit; write the exception into the commit message explicitly, naming battle.cpp:95 as the reason a content-only change moves a battle hash. (c) THEN `python3 tools/pbm2svg.py --all`, `python3 tools/build_wiki.py`, `tools/build_manual.sh` - now nine SVGs, not two.
9. STEP 15 (gates): as written, one at a time. Add: confirm the sprite gate actually RAN (tools/check.sh:229 prints 'SKIPPING the sprite gate' to stderr and still says GATE OK) - the plan flags this in risks but does not put it in the steps.
10. STEP 16 (guide agent, LAST): correct the brief. It must name TEN guide.typ lines, not five - 787, 799, 820, 1090, 1241 in Spanish and 801, 813, 824, 1101, 1261 in English - plus tools/wiki/page.html:7 (the meta description, which the plan missed entirely), :163, :299, :302, :309, :316, and docs/CONTEXT.md:99, :179. And it must be told that NINE screen SVGs changed, seven of them battle screens whose foe creature is now a different species, so any booklet or website prose naming the creature in those captures is also false.